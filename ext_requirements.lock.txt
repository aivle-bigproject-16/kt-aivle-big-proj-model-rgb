# EXT 배포 환경 잠금 — 2026-08-06 실측 (Colab A100 80GB)
# 이 조합에서 golden_fixture_deploy.json 이 생성됐다.
# 바꾸면 `python ext_infer.py --verify-fixture` 를 반드시 다시 돌릴 것.
# python 3.12.13 · Linux-6.6.122+-x86_64-with-glibc2.35
# GPU NVIDIA A100-SXM4-80GB · driver 580.82.07 · CUDA 12.8

torch==2.11.0+cu128
torchvision==0.26.0+cu128
transformers==5.14.1
tokenizers==0.22.2
huggingface-hub==1.23.0
accelerate==1.14.0
safetensors==0.8.0
pillow==11.3.0
numpy==2.0.2
scipy==1.16.3
qwen-vl-utils==0.0.14

# 2단 VLM(Qwen)은 ext_infer.py 에 없다 — qwen-vl-utils 는 평가 노트북용이다.
# opencv 는 추론 경로에서 쓰지 않는다(기록용): opencv-python==5.0.0.93
