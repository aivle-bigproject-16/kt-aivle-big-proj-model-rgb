# Model Card — EXT / RGB 외관 검사

원통형 리튬셀 표면 사진에서 결함 프레임을 걸러내고 위치·크기를 리포트로 넘기는 파이프라인입니다.
`ai-infer` 가 이 문서만 보고 재현할 수 있도록 요청서 템플릿 순서로 작성했습니다.

- 기계가 읽을 형태 → [`ext.model_card.json`](ext.model_card.json)
- 재현 스펙 (코드 계약·질의어 전문·실행 순서) → [`EXT_SPEC.md`](EXT_SPEC.md)
- 검증 노트북 → `battery_ext_modelcard_check.ipynb` (11셀, 2026-08-06 전량 실행)

| | |
| --- | --- |
| 최종 갱신 | **2026-08-06** (배포 경로 실측 · 셀 단위 판정 신설) |
| 모달 | EXT / RGB (CT 는 `model-ct` 레포) |
| 구조 | **OWLv2(검출) → Qwen2.5-VL(해설) 2단.** 학습 0회 · 라벨 0장 |
| 외부 API | **0건** (전량 로컬 GPU) |
| 배포 산출물 | **`ext_infer.py` 단일 모듈** — §10 |
| 판정 성능 (프레임) | **P 0.855 / R 1.000 / F1 0.922** (300장, 배포 경로) |
| 판정 성능 (**셀**) | **P 1.000 / R 1.000** (결함 12셀 · 정상 12셀) — §5.7 |
| 셀당 지연 | **12.2초** (30프레임 균등 추출) — 전량 검사 시 1.8분 |
| 🔴 남은 blocker | 지연이 목표(4초)의 **3배** · 계약 enum 2건 — §13 |

> ### 이 카드의 주어가 YOLO 가 아닌 이유
>
> 요청서 §3 은 RGB 를 YOLO(`Damaged`/`Pollution` → `CRACK`/`SPOT`)로 상정하고 쓰였습니다.
> 사람이 직접 찍은 같은 시험지에서 점 recall 이 YOLO **0.089** vs OWLv2 **0.818** (9.2배)였고,
> 유형별로는 YOLO 가 **녹·부식 0.024 · 긁힘 0.000 · 들뜸 0.000** 으로 해당 유형을 사실상 못 잡았습니다.
> 기존 YOLO 경로는 라벨이 깨져 천장에 막혀 있습니다(§9.3).
> YOLO 는 **§14 대조군**으로 남깁니다(지우지 않습니다).
>
> ⚠️ 대신 계약과 안 맞는 것이 생겼습니다 — §5.3 `class_map` 과 §13 #1 을 보세요.

---

## 0. 요청 항목 대조표

| 요청 (§) | 상태 | 값 / 위치 |
| --- | --- | --- |
| 1.1 식별 | ✅ | §1 — `weight_sha256` 대응물 = **HF `revision` 고정**, 두 모델 다 확정 |
| 1.2 라이브러리 버전 | ✅ | §2 — 실측 완료. `transformers >=5.13.1,<6` (두 버전 동일 확인) |
| 1.3 입력 전처리 | ✅ | §3 — **RGB 도 ROI(셀 크롭) 전처리가 있습니다** + 정사각 패딩 필수 |
| 1.4 추론 파라미터 | ✅ | §4 |
| 1.5 출력 매핑 | 🔴 **미결** | §5.3 — 6유형을 계약 enum 2종(`CRACK`·`SPOT`)에 못 담습니다 |
| 3 RGB `sahi.enabled` | ✅ | §4 — SAHI 자체를 안 씁니다. 대응 항목은 `TILES = 1` |
| 3 RGB ROI 여부 | ✅ | §3.1 — 있습니다. 크기는 **고정 아님** |
| 5 골든 픽스처 | ✅ | §7 — **20장 생성 · 회귀 검증 통과** |
| 6 model_card.json | ✅ | [`ext.model_card.json`](ext.model_card.json) |
| 7 confidence 산식 | 답변 | §12 |
| — 배포 모듈 | ✅ | §10 — **`ext_infer.py`** (입력 원본 · 출력 원본좌표 · 디스크 쓰기 0) |
| — 셀 단위 판정 | ✅ **성립함** | §5.7 — flag율 ≥ 70% · P 1.000 / R 1.000 (0806 신설) |
| — 배포 지연 | ⚠️ 개선 | §10 — 셀당 **12.2초** (목표 4초의 3배. 전량 검사 1.8분에서 9배 단축) |
| — 입력 규격 | 🔴 **계약 필요** | §10.2 — **원본 JPEG 재인코딩 금지**(검출 수가 53% 바뀝니다) |

---

## 1. 식별

| 항목 | 값 |
| --- | --- |
| `weight_file` | **없음** — 학습된 가중치가 아니라 사전학습 모델 2개를 그대로 씁니다 |
| `detector_model_id` | `google/owlv2-large-patch14-ensemble` |
| `detector_revision` | **`95e26936e865f87db1742128404b3c035d47d89d`** |
| `vlm_model_id` | `Qwen/Qwen2.5-VL-7B-Instruct` |
| `vlm_revision` | **`cc594898137f460bfe9f0759e9844b3ce807cfb5`** |
| `weight_sha256` | **개념 없음** → 아래 대응물 |
| `train_run` | **없음** (학습 0회) |
| `labels_used` | **0** |
| `source_repo` | `aivle-bigproject-16/kt-aivle-big-proj-model-rgb` |
| `source_commit` | ❓ 미기입 (§13 #6) |
| `trained_at` | 해당 없음 |

> ### `weight_sha256` 의 대응물 = **HF `revision` 고정**
>
> 가중치 파일이 없으니 해시로 검증할 대상도 없습니다. 대신 **HuggingFace 리포지토리는 갱신될 수
> 있습니다.** `from_pretrained(MODEL_ID)` 만 쓰면 어느 날 다른 가중치를 받아도 조용히 돌아갑니다.
>
> **두 모델 다 `revision='<위 해시>'` 로 고정하세요.** 이것이 이 파이프라인에서 `weight_sha256` 이
> 하는 일을 대신합니다.
>
> ✅ **2026-08-06 확인: 로컬 스냅샷 해시 = 원격 `main` 해시** (`revisions.json`).
> 즉 지금까지 측정한 모든 수치가 위 두 리비전에서 나온 값입니다. 코드에 박기만 하면 됩니다.

---

## 2. 라이브러리 버전

### 2.1 실측 환경 (2026-08-06)

| 패키지 | 버전 | 서빙 필요 |
| --- | --- | --- |
| `python` | `3.12.13` | ✅ |
| `torch` / CUDA | `2.11.0+cu128` / `12.8` | ✅ |
| **`transformers`** | **`5.13.1`** (측정본) | ✅ OWLv2 · Qwen 양쪽 |
| `tokenizers` | `0.22.2` | ✅ |
| `huggingface-hub` | `1.23.0` | ✅ |
| `accelerate` | `1.14.0` | ✅ (`device_map='auto'`) |
| `qwen-vl-utils` | `0.0.14` | ✅ (2단만) |
| `numpy` · `pillow` | `2.0.2` · `11.3.0` | ✅ |

**측정 GPU: `NVIDIA A100-SXM4-80GB` · driver `580.82.07`.** 전체 목록은 `{OUT}/modelcard/env.json`.

### 2.2 `transformers` 는 범위로 둡니다 — `>=5.13.1,<6`

한 버전에 못 박지 않는 근거가 실측으로 있습니다.

| (평가 경로 · thr 0.08) | `5.13.1` | `5.14.1` |
| --- | --- | --- |
| P / R / F1 | 0.873 / 0.995 / 0.930 | **소수점까지 동일** |
| 오탐 / 놓침 | 29 / 1 | **동일** |
| 후처리 API | `post_process_grounded_object_detection` | 동일 |

**두 버전에서 운영점이 완전히 같았습니다.** = 이 범위 안에는 버전 위험이 없다는 실측입니다.
(이 비교는 **평가 경로**에서 두 번 돌린 것입니다. 배포 운영점은 §6.1 을 보세요.)

> ⚠️ **검증된 것은 집계 지표(300장 운영점)까지입니다.**
> 골든 픽스처는 매 실행마다 재생성되므로 **박스 단위 교차버전 일치는 미검증**입니다.
> 엄밀히 보려면 픽스처 파일을 버전별로 따로 보관하고 §7 회귀만 돌려야 합니다.

> ### ⚠️ 그래도 버전을 비워두면 안 되는 이유
>
> 후처리 API 이름이 버전 간 다릅니다 — `post_process_grounded_object_detection` ↔
> `post_process_object_detection`. 코드는 지연 평가(`or`)로 둘 다 처리하지만, **더 위험한 건
> 조용한 쪽입니다.** 후처리 구현이 바뀌어 점수 스케일이 달라지면 §6 의 `thr 0.08` · `thr 0.12`
> 운영점이 **에러 없이 무효**가 됩니다.

**메모리**: Qwen2.5-VL-7B 가 bf16 로 ~16GB, OWLv2-large 가 별도. 측정은 A100 80GB 에서 했습니다.
배포 GPU 가 T4/L4 면 **들어가는지부터** 확인해야 합니다(§13 #4).

---

## 3. 입력 전처리

| 항목 | 값 |
| --- | --- |
| 원본 규격 | `1920 x 1080` · `RGB_cell_cylindrical` · 759 ID 전부 동일 |
| `color_space` | `RGB` |
| 셀당 프레임 | **~270** (360° 회전 촬영) |
| **모델 입력** | **셀 크롭 → 정사각 패딩.** 실측 평가셋 크롭 크기 **1464 x 895** |
| `imgsz` | 해당 없음 — `Owlv2Processor` 가 처리 |
| `TILES` | `1` (크롭 통짜) |
| 크기 고정? | **아니오** — 매 이미지 추정값 |

### 3.1 셀 크롭 — 요청서 §3 "RGB 도 ROI 전처리가 있는가" 에 대한 답: **있습니다**

예전 문서의 "EXT 는 crop 이 없어 이미지 = ROI = 전체 프레임" 은 **더 이상 맞지 않습니다.**
v4.2 부터 셀 크롭이 들어갔습니다.

| 파라미터 | 값 |
| --- | --- |
| 방식 | HSV 채도 기반 (배경=무채색 · 셀=유채색 전제) |
| `SAT_TH` | `40` |
| `DENS_TH` | `0.10` (행·열 마스크 밀도 — 흩어진 노이즈에 강건) |
| `PAD` | `0.10` (결함 보존 Damaged 100% / Pollution 99.7%) |
| `MIN_AREA` / `MAX_AREA` | `0.02` / `0.90` |
| 채도로 안 갈릴 때 | 밝기(V) 폴백 |
| 추정 실패 시 | **원본 전체 `(0,0,1,1)` 폴백** — 파이프라인이 멈추지 않음 |
| 정의 위치 | `battery_ext_crop_retrain.ipynb` §1 (학습·추론 공용) |
| 크롭본 생성 | `battery_ext_unsup.ipynb` §0 |

**리포트 좌표는 원본 기준이어야 합니다.** 크롭 좌표를 그대로 내보내면 프론트에서 박스가 어긋납니다.
`to_orig_coords()` 로 되돌리세요(왕복 변환 자체 검증 있음).

> ❓ **크롭을 누가 하는지 정해야 합니다** — `ai-infer` 가 원본을 받아 크롭하는지, 크롭본을 받는지에
> 따라 좌표 원복 책임이 갈립니다(§13 #5).

### 3.2 정사각 패딩 — 생략하면 좌표가 통째로 어긋납니다

**OWLv2 전처리가 이미지를 정사각으로 패딩합니다.** 직사각을 그대로 넣으면 반환 좌표가
패딩된 캔버스 기준이라 **박스가 어긋납니다. 에러는 안 나고 숫자만 틀립니다.**

→ **우리가 먼저 정사각 패딩**해 내부 패딩을 무연산으로 만들고, `target_sizes` 에 `(S, S)` 를 줍니다.

```python
S  = max(sub.size)                       # 긴 변
sq = Image.new('RGB', (S, S), (0, 0, 0)) # 검정 캔버스
sq.paste(sub, (0, 0))                    # 좌상단 정렬
...
sizes = torch.tensor([[S, S]] * len(blk))
```

| 항목 | 값 |
| --- | --- |
| 패딩 | 정사각, fill `(0,0,0)`, anchor **좌상단** |
| `target_sizes` | `(S, S)` — 패딩 후 정사각 크기 |

> ⚠️ 이 패딩이 §10 지연의 원인이기도 합니다. `1464 x 895` 가 `1464²` 가 되어 연산량이 1.6배입니다.

### 3.3 2단(VLM) 입력 — 패널 구성

| 파라미터 | 값 |
| --- | --- |
| `MARGIN` | `0.35` (박스 최대변의 35% 여유) |
| `PANEL` | `336` px |
| `CAP_ZONE` | `0.22` (셀 높이 상단 22% = 금속캡 구역) |
| 구성 | `[ 기준 패치 | 확대 크롭 ]` 한 장 |
| 기준(몸통) | 같은 셀에서 박스가 가장 먼 곳을 같은 크기로 |
| 기준(금속캡) | 사람이 확인한 **정상 캡 갤러리 16장** |

> 🔴 **`PIL.thumbnail()` 을 쓰지 마세요 — 확대를 안 합니다.** 작은 크롭이 336px 칸 구석에 붙고
> 나머지가 흰 여백이 되어 VLM 이 사실상 빈 칸을 봅니다. **`resize`** 를 쓰세요.

---

## 4. 추론 파라미터

```yaml
detector:                                  # ① OWLv2
  model_id: google/owlv2-large-patch14-ensemble
  revision: 95e26936e865f87db1742128404b3c035d47d89d
  dtype: fp32
  tiles: 1
  batch_size: 16                           # 실측 최적 (§10)
  topk: 50                                 # 내부 수집 상한. 사후 필터용으로 넓게
  drop_neg: true
  band_filter: false                       # 기각 (§9.2)

stage1_gate:                               # 프레임 결함/정상 판정 ← 판정에 쓰이는 건 이것뿐
  threshold: 0.08
  min_boxes: 8

stage2_locate:                             # 리포트에 실을 박스 선정
  threshold: 0.12
  top_n: 6

vlm:                                       # ② Qwen2.5-VL — 판정에 쓰이지 않음
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
  revision: cc594898137f460bfe9f0759e9844b3ce807cfb5
  dtype: bfloat16
  device_map: auto
  mode: per_item                           # montage 아님
  do_sample: false                         # greedy
  max_new_tokens: 120
```

**SAHI 는 쓰지 않습니다** (요청서 §3 `sahi.enabled` 대응). 대응 항목은 `TILES = 1` 이고,
작은 결함을 놓치면 `3`(세로 3등분)이 업그레이드 경로입니다. 현재 미사용.

### 4.1 2단으로 나눈 이유 — 임계 하나로 통일하면 안 됩니다

| 단 | 임계 | 역할 | 실측 |
| --- | --- | --- | --- |
| ① 게이트(프레임) | **0.10** · N≥8 | 결함 프레임을 안 놓치기 | P 0.855 / **R 1.000** / F1 0.922 (300장) |
| ① 게이트(**셀**) | 30프레임 중 **flag율 ≥ 70%** | 셀 단위 폐기 판정 | **P 1.000 / R 1.000** (각 12셀) — §5.7 |
| ② 위치 | **0.12** · 상위 6 | 박스 위치가 맞아야 리포트가 성립 | 점 recall **0.818** (순열 2.51×) |

> 🔴 **프레임 임계가 0.08 → 0.10 으로 바뀐 이유**: 평가는 **이미 잘린 크롭 이미지**(JPEG 2세대)에서,
> 배포는 **원본 → 크롭**(1세대)에서 돕니다. 재압축은 미세 질감을 뭉개고 그 질감이 저점수 검출을
> 만들므로, 배포 경로에서 박스가 **53% 더** 나옵니다(33.1 vs 21.6 박스/장). §10.2 참조.

게이트는 **박스 개수만** 세므로 위치 정확도가 필요 없습니다. 위치가 필요한 ②만 0.12 를 씁니다.
**역할 분리로 둘 다 최적입니다.**

> 🔑 **게이트는 태그도 종류도 안 씁니다. 임계 이상 박스의 개수만 셉니다.**
> 그래서 유형 분류가 실패해도(§9.2) 판정 성능은 영향을 받지 않습니다.

### 4.2 질의어 = 이 파이프라인의 실질 "라벨"

**총 35개** (결함 25 + 정상 10). 이 수가 §10 지연에 직접 영향합니다.

```python
QUERY_MAP = {
  '녹·부식':      ['rust', 'orange brown discoloration', 'brown stain on surface',
                   'oxidation on surface', 'reddish brown spot'],
  '벗겨짐·박리':  ['peeling plastic wrap', 'peeled off label',
                   'exposed metal under wrapper', 'wrapper coming off',
                   'peeling film edge'],
  '파손·찢김':    ['tear in plastic film', 'crack on surface', 'puncture hole', 'dent'],
  '긁힘·스크래치': ['scratch', 'scratch mark', 'scratched metal surface', 'scuff mark'],
  '들뜸':         ['air bubble under film', 'wrinkled plastic wrap', 'lifted film edge'],
  '오염·이물질':  ['dirt stain', 'smudge', 'white residue', 'foreign particle on surface'],
}

NEG_MAP = {   # 방해 질의 — 정상 요소에 이름을 줘서 결함 질의를 훔쳐가지 않게 한다
  '정상:인쇄·각인': ['printed text', 'printed letters on label', 'engraved serial number'],
  '정상:반사':      ['light reflection', 'specular highlight'],
  '정상:금속캡':    ['metal cap', 'battery terminal'],
  '정상:테두리':    ['bottom edge of cylinder', 'rim of metal can', 'silhouette edge'],
}
DROP_TAGS = {'정상:인쇄·각인', '정상:반사', '정상:테두리'}
```

설계 규칙(OWLv2 는 CLIP 계열 텍스트 인코더): ① 영어 · 짧은 명사구 ② 추상어(`defect`·`anomaly`)는
안 먹힘 — 눈에 보이는 물체/현상으로 ③ 한 유형에 표현 여러 개를 OR ④ **한국어 키는 평가셋 유형과
같은 이름**(유형별 recall 을 내려면 어휘가 같아야 함).

> 🔴 **`정상:금속캡` 을 `DROP_TAGS` 에 넣지 마세요.** 구조물 판정으로 지우면 오탐 5개를 지우고
> **진짜 결함 9개를 잃습니다**(순손실). 캡 위의 부식은 결함입니다. 이름만 붙이고 남깁니다.

> **질의어 교체는 "찾기"가 아니라 "이름"만 바꿉니다.** 녹 질의를 금속 지향 → 색·산화 지향으로
> 바꾸니 녹 태그 정확도가 0.031 → **0.225** (순열 2.46× ✅)가 됐고 **재학습은 0회**였습니다.
> 그런데 **게이트·검출률은 소수점까지 동일**했습니다. 질의어는 라벨 교체에 해당합니다.

### 4.3 VLM 프롬프트 — v8 (확정)

```
왼쪽은 같은 셀의 다른 부위다. 오른쪽이 검사 대상이다.
왼쪽에는 없고 오른쪽에만 있는 것을 찾아라.

오른쪽에서 보이는 것을 고르라. 설명하지 말고 아래 세 줄만 답하라.

종류  0 없음  1 녹·부식  2 벗겨짐  3 찢김·파손  4 긁힘  5 들뜸  6 오염·이물질
크기  A 사진 절반 이상   B 사진의 1/4쯤   C 손톱만 함   D 점 하나

종류: (숫자)
크기: (알파벳)
본것: (한 문장)
```

> ### 🔴🔴 프롬프트 설계 철칙 — "베낄 것을 남기지 않는다"
>
> 7B 모델은 프롬프트에서 눈에 띄는 텍스트를 그대로 출력에 복사합니다. **5회 확인됐습니다.**
>
> | 회차 | 증상 | 대응 |
> | --- | --- | --- |
> | v3 | "표면에 갈색 부식이 보입니다" ×8 복사 | 정상 특징 나열 제거 |
> | v4 | 전부 "없음" | 왼쪽을 "정상 기준"이라 부르는 것 제거 |
> | v5 | `<오른쪽에 보이는 것>` 플레이스홀더를 그대로 출력 | 예시 JSON 제거 |
> | v7 | 주의사항 목록을 통째로 복창 | 목록 제거 |
> | v7 | **채점 기준표를 종류 칸에 복사**(`금속이 드러남` 111건) | 기준표 제거 |
>
> **틀을 하나 없애면 다음으로 눈에 띄는 텍스트를 베낍니다.** "덜 보여준다"가 아니라
> **"베낄 것을 아예 남기지 않는다"** 가 규칙입니다.

> 🔴 **한국어를 생성시키지 마세요.** v7 에서 `베지먼트`·`베져짐`·`베기짐` **118건(19%)** 이
> '벗겨짐' 의 깨진 토큰이었습니다. 7B 가 안정적으로 못 뱉는 단어를 요구한 설계 잘못입니다.
> **번호·알파벳으로만 답하게** 하니 0건이 됐습니다.

> **몽타주(SoM 번호 참조)는 실패했습니다 — `per_item` 이 기본입니다.** 작은 VLM 이 번호↔칸 매핑에
> 약해, 캡이 없는 칸을 "금속캡 각인" 이라 하고 근거 문장이 9칸에 글자까지 동일했습니다.

---

## 5. 출력 매핑

### 5.1 검출기 출력 (①)

```python
detect(paths, thr, topk, tiles=1, bs=16, drop_neg=True)
  → {path: [(x1, y1, x2, y2, score, tag, query), ...]}   # score 내림차순
```

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `x1,y1,x2,y2` | float | **크롭 이미지** 픽셀 좌표 (타일 오프셋 복원 완료) |
| `score` | float | OWLv2 질의 매칭 점수. **질의 간 스케일이 다름** |
| `tag` | str | 한국어 유형 6종 또는 `정상:*` |
| `query` | str | 이긴 영어 질의 (진단용) |

| 항목 | 값 |
| --- | --- |
| `bbox_format` | `xyxy` |
| `coordinate_reference` | `crop_image` — **원본 기준 리포트는 `to_orig_coords()` 필요** (§3.1) |

> 🔴 **`score` 는 질의마다 스케일이 다릅니다.** 전역 argmax 를 하므로 `tag` 는 "가장 잘 맞는 결함"이
> 아니라 **"가장 점수가 잘 나오는 질의"** 입니다. 게이트는 개수만 세므로 무관하지만,
> **`tag` 를 유형으로 신뢰하면 안 됩니다** (§9.2).

### 5.2 결함 유형 6종

`녹·부식` · `벗겨짐·박리` · `파손·찢김` · `긁힘·스크래치` · `들뜸` · `오염·이물질`

### 5.3 🔴 `class_map` — **계약 enum 과 안 맞습니다**

| | |
| --- | --- |
| 계약 §6.5 RGB enum | `CRACK` · `SPOT` (2종) |
| 이 파이프라인 | 위 6종 |
| `class_map` | ❓ **규칙 없음** |

`파손·찢김` → `CRACK`, `오염·이물질` → `SPOT` 까지는 자연스럽지만 **`녹·부식`(사람 점 최다) ·
`긁힘·스크래치` · `벗겨짐·박리` · `들뜸` 이 갈 곳이 없습니다.** 계약 전체 enum 에 `SWELLING` 이 있어
`들뜸` 은 거기로 볼 여지가 있으나 RGB 는 2종만 쓰기로 돼 있습니다.

**선택지는 둘입니다** — ① 6종을 2종으로 뭉갠다(정보 손실) ② 계약 enum 에 `CORROSION`·`PEELING` 을
추가한다. **계약 쪽 결정이 필요합니다**(§13 #1).

> 다만 **판정(게이트)은 이 결정과 무관하게 이미 동작합니다.** 게이트는 태그를 안 쓰기 때문입니다.
> 막히는 건 리포트의 유형 표기뿐입니다.

### 5.4 리포트 JSON — 산출 규격

**리포트를 쓰는 것은 LLM 이고, 이미지를 볼 수 없습니다.** 우리가 틀린 값을 넘기면 LLM 은
**그대로 사실로 받아 적습니다.** 스스로 의심할 방법이 없습니다.
→ 값만 넘기지 않고 **그 값이 어디까지 검증됐는지**를 같이 넘깁니다.

```
{OUT}/report_input/
    _contract.json        ← LLM 프롬프트 맨 앞에 붙일 계약서 (1개)
    cell_0041.json        ← 셀당 1개
```

**LLM 에는 `_contract.json` + 해당 `cell_XXXX.json` 을 같이 줍니다.**
계약서 없이 주면 LLM 은 유형후보 1등을 확정으로 써버립니다.

```json
{
  "셀": "0041",
  "검사_프레임수": 4,
  "주의": "프레임은 같은 셀을 360도 회전하며 찍은 것이다. 같은 결함이 여러 프레임에 나올 수 있다.",
  "프레임": [{
    "파일": "EXT__RGB_cell_cylindrical_0041_151__5219a299.jpg",
    "판정": "결함",
    "판정_신뢰": "높음",
    "판정_근거": {"임계": 0.10, "박스수": 18, "기준": "8개 이상"},
    "결함": [{
      "위치": {"구역": "몸통", "셀_상단에서": 0.73, "bbox": [0, 415, 297, 893]},
      "위치_신뢰": "높음",
      "크기": {"셀면적비_퍼센트": 10.83, "구분": "큼"},
      "크기_신뢰": "중간",
      "유형후보": ["긁힘·스크래치", "녹·부식", "파손·찢김"],
      "유형_신뢰": "낮음",
      "유형_주의": "검증 미통과. 확정 표현 금지"
    }]
  }]
}
```

| 경로 | 타입 | 값 | 신뢰 |
| --- | --- | --- | --- |
| `셀` | str | 파일명 `cylindrical_(\d+)_` 에서 추출 | — |
| `프레임[].판정` | str | `결함` \| `정상` | **높음** (300장 F1 0.930) |
| `프레임[].판정_근거` | obj | `{임계 0.10, 박스수 N, 기준 "8개 이상"}` | — |
| `셀_판정` | str | `결함` \| `정상` (flag율 ≥ 70%) | **높음** (§5.7) |
| `flag율` | float | 검사 프레임 중 결함 비율 | — |
| `전체_프레임수` · `검사_프레임수` | int | 전량 / 균등 추출한 수 | — |
| `결함[].위치.구역` | str | `금속캡` \| `몸통` | **높음** |
| `결함[].위치.셀_상단에서` | float | 0.0~1.0 | **높음** |
| `결함[].위치.bbox` | int[4] | `[x1,y1,x2,y2]` | **높음** |
| `결함[].크기.셀면적비_퍼센트` | float | 박스면적/셀면적×100 | 중간 |
| `결함[].크기.구분` | str | `큼`(≥1.0%) \| `작음` | 중간 |
| `결함[].유형후보` | str[] | **순위만. 점수 없음** | **낮음** |

**결함 배열은 `판정 == "결함"` 일 때만 채워지고, 프레임당 최대 6개**(점수 상위).

### 5.5 🔴 유형후보에 점수를 넣지 않습니다

초기 버전은 점수를 함께 냈습니다 (`["긁힘·스크래치", 0.84]`). **폐기했습니다.**

| 점수 구간 | n | 평균 점수 | 실제 적중 | 차이 |
| --- | --- | --- | --- | --- |
| 0.85~1.01 | **176 / 210 (84%)** | **0.954** | **0.477** | **−0.477** |

거의 모든 박스에 "95% 확신" 이라 적고 **절반이 틀립니다.** LLM 은 그림을 못 보므로 그 숫자를 그대로
"확실히 녹입니다" 로 옮깁니다 = **없는 확신을 만들어 파는 것**입니다.
→ **순위만 넘깁니다.** 후보는 `[색규칙/VLM 1등, 2등, (없으면) 검출기 태그]` 순, 최대 3개.

### 5.6 등급 — 계산은 하되 **내보내지 않습니다**

```python
KIND_UP = {2, 3}          # 벗겨짐·파손은 금속 노출로 이어진다
grade = 0 if kind == 0 else min(5, SIZE_G[size] + (1 if kind in KIND_UP else 0))
```

VLM 에 등급을 **묻지 않고** 코드가 계산합니다 — 등급은 "얼마나 심한가" 라는 주관 판단이라 모델이
가장 쉽게 뭉갭니다(실제로 뭉갰습니다). 크기·종류는 눈에 보이는 사실이라 훨씬 안정적입니다.

**그런데 이 등급도 리포트에 안 내보냅니다.** 결함 프레임 박스의 19.7% 가 4등급↑ 인데
**무결함 프레임은 20.2%** 로 분리력이 0 이었습니다(§9.2). §5.4 스키마에 `등급` 필드가 없는 이유입니다.

### 5.7 셀 단위 `verdict` — ✅ **성립합니다** (0806 신설)

이전 판(0805)에는 "무결함 셀이 없어 셀 단위 판정이 성립하지 않는다"고 적혀 있었습니다.
**다른 방식으로 풀렸습니다.** 셀 라벨 없이도 셀을 가를 수 있습니다.

#### 규칙

```
셀당 30프레임을 균등 추출 → 각 프레임을 게이트(thr 0.10 · N≥8)로 판정
flag율 = 결함 프레임 / 검사 프레임
flag율 ≥ 0.70  →  셀 = 결함
```

#### 근거 — 두 집단이 완전히 갈립니다

| 집단 | 표본 | flag율 |
| --- | --- | --- |
| **결함 확정 셀** (평가셋에 결함 라벨 프레임이 있는 셀) | 12개 | 최소 **80%** · 중앙 100% |
| **정상 추정 셀** (무결함 프레임만 라벨된 셀) | 12개 | 최대 **50%** · 중앙 30% |

**간격 +30%p.** 사이가 비어 있습니다.

#### 왜 셀 단위가 프레임 단위보다 깨끗한가

**프레임 오탐은 무작위**입니다 — 정상 셀에서도 30%쯤이 흩어져 걸립니다(프레임 P 0.855 와 일치).
**결함은 지속적**입니다 — 회전 각도가 바뀌어도 80~100% 프레임에서 계속 보입니다.
프레임을 모으면 **무작위 잡음은 평균으로 씻기고 지속 신호만 남습니다.**

| | Precision | Recall |
| --- | --- | --- |
| 프레임 단위 | 0.855 | 1.000 |
| **셀 단위** | **1.000** | **1.000** |

#### K(검사 프레임 수) 선택 — 지연과의 교환

| K | 셀당 | 셀 recall | 셀 precision |
| --- | --- | --- | --- |
| 10 | 4.1초 | 0.993 | 0.975 |
| 20 | 8.1초 | 0.998 | 0.998 |
| **30** | **12.2초** | **1.000** | **1.000** ← 기본값 |

불량 게이트라 recall 우선으로 30을 기본값으로 뒀습니다. **4초가 계약이면 K=10** 입니다(recall 0.993).
`ext_infer.Config.cell_sample_k` · `cell_flag_rate` 상수 두 개입니다.

> ⚠️ **표본이 셀 12개씩입니다.** `1.000` 은 95% 상한 오차율이 ~22% 라 근거가 얇습니다.
> **견고한 발견은 "간격 +30%p"** 입니다.
> 정상 쪽은 "무결함 프레임만 라벨된 셀" 이라 셀 전체가 정상이란 보장이 없습니다
> (결함 쪽은 라벨이 확정이라 **recall 은 신뢰 가능**).
> → 계약상 셀 판정을 쓰려면 **셀 단위 정답 라벨**로 재확인이 필요합니다(§13 #3).

---

## 6. 운영점

| 용도 | 임계 | 규칙 | 근거 |
| --- | --- | --- | --- |
| **게이트(프레임)** | `thr = 0.10` | 박스 **8개 이상** → 결함 프레임 | P 0.855 / R 1.000 / F1 0.922 (배포 경로 300장) |
| **게이트(셀)** | — | 30프레임 중 **flag율 ≥ 70%** | P 1.000 / R 1.000 (각 12셀) |
| **위치(리포트)** | `thr = 0.12` | 점수 상위 **6개** | 점 recall 0.818, 순열 2.51× |
| `topk` (내부) | `50` | 사후 필터용 넓게 수집 | |
| 밴드 필터 | **사용 안 함** | — | 기각 (§9.2) |

### 6.1 프레임 게이트 운영점 (배포 경로 · 300장)

| 설정 | P | R | F1 | 오탐 | 놓침 |
| --- | --- | --- | --- | --- | --- |
| thr0.05 · N≥40 | 0.970 | 0.980 | **0.975** | 6 | 4 |
| thr0.12 · N≥8 | 0.965 | 0.960 | 0.963 | 7 | 8 |
| **thr0.10 · N≥8** ← 확정 | 0.855 | **1.000** | 0.922 | 34 | **0** |
| thr0.08 · N≥12 | 0.804 | 1.000 | 0.891 | 49 | 0 |

**F1 최대점(0.975)이 아니라 놓침 0 지점을 택했습니다.**
불량 게이트에서 놓침 4장(결함 셀 4개 통과)은 오탐 34장(사람이 34장 더 봄)과 비교 대상이 아닙니다.
**그리고 셀 단위로 모으면 그 오탐 34장이 씻깁니다**(§5.7) — 프레임 오탐을 관대하게 둘 수 있는 이유입니다.

### 6.2 임계를 0.08 → 0.10 으로 올린 이유 (경로 차이)

| 경로 | 입력 | thr | P | R | F1 | 오탐 | 놓침 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 평가 (노트북) | 이미 잘린 크롭 (JPEG 2세대) | 0.08 | 0.873 | 0.995 | 0.930 | 29 | 1 |
| 평가 경로 코드를 배포 입력에 그대로 | 원본→크롭 (1세대) | 0.08 | **0.739** | 1.000 | 0.850 | **71** | 0 |
| **배포 (확정)** | 원본→크롭 (1세대) | **0.10** | **0.855** | **1.000** | **0.922** | 34 | **0** |

같은 코드·같은 라벨인데 **입력 이미지의 JPEG 세대만 달라도 오탐이 29 → 71** 로 뜁니다.
원인과 계약은 §10.2 에 있습니다.

### 6.3 표본을 2배로 늘렸을 때의 변화 (견고성 근거)

| | 150장 (0805) | 300장 (0806) |
| --- | --- | --- |
| P | 0.876 | 0.873 |
| R | **1.000** | 0.995 |
| F1 | 0.934 | **0.930** |

**표본이 2배가 됐는데 F1 이 0.004 밖에 안 움직였습니다** — 운영점이 견고하다는 증거입니다.
(위 표는 **평가 경로** 기준입니다. 배포 경로 수치는 §6.1.)

---

## 7. 골든 픽스처 (회귀 테스트)

배포·리팩터링·라이브러리 업그레이드 후 **같은 입력에서 같은 출력이 나오는지** 확인하는 고정 표본입니다.
전처리가 어긋나도 서버는 200 을 반환하고 성능만 조용히 나빠지므로, 이게 유일한 자동 방어선입니다.

### 7.1 픽스처는 **2종**입니다 — 경로가 다르면 입력이 다릅니다

같은 셀 20장이지만 **입력 이미지가 다릅니다.** 평가 픽스처는 이미 잘린 크롭본(2세대 JPEG),
배포 픽스처는 카메라 원본(1920×1080, 1세대)입니다. §6 에서 본 것처럼 이 차이가 검출 수를
53% 바꾸므로, **서로 바꿔 쓸 수 없습니다.**

| | `golden_fixture.json` | `golden_fixture_deploy.json` |
| --- | --- | --- |
| 무엇을 지키나 | **검출기** 회귀 | **배포 경로 전체** 회귀 (크롭 포함) |
| 입력 | 평가셋 크롭본 (354×1074 등) | **원본 1920×1080** |
| 운영점 | thr 0.08 | **thr 0.10** ← 실제 배포값 |
| 굳힌 값 | 임계별 박스 수 · top5 (`bbox`·`score`·`tag`) | `gate` · `n_box` · `crop_offset`/`size` · `defects` |
| 좌표계 | 크롭 기준 | **원본 기준** |
| 돌리는 법 | `battery_ext_modelcard_check.ipynb` §8 | **`python ext_infer.py --verify-fixture`** |
| 표본 | seed 7, 평가셋 300장 | 같은 20장의 **원본** |

> 🔴 **배포 검증에 쓸 것은 `golden_fixture_deploy.json` 입니다.** 평가 픽스처는 thr 0.08 로
> 만들어졌으므로 지금 설정(0.10)을 검증할 수 없습니다. 잘못 들이대면 도구가 🔴 로 막습니다.

> ⚠️ 평가 픽스처의 `_meta.revision` 은 생성 당시 기록되지 않았습니다(`null`).
> 같은 런의 `revisions.json` 이 로컬 스냅샷 1개(`95e26936…`)임을 보이므로 **사후 확정**해
> 채워 넣었고, 그 경위를 `_meta.revision_출처` 에 남겼습니다.

### 7.2 배포 회귀 — 명령 한 줄

```bash
python ext_infer.py --verify-fixture \
    --fixture golden_fixture_deploy.json --images <원본 20장 폴더>
# 회귀 있으면 exit 1  ← CI 에 그대로 걸 수 있습니다
```

이미지 sha256 을 먼저 대조하므로 **입력이 바뀐 것**과 **모델이 바뀐 것**을 구분해 줍니다.
`_meta.thr_gate` · `_meta.revision` 이 현재 `Config` 와 다르면 비교 전에 🔴 를 띄웁니다.

### 7.3 통과 기준

| 항목 | 배포 픽스처 | 평가 픽스처 |
| --- | --- | --- |
| 게이트 판정 (`결함`/`정상`) · 박스 개수 | **완전 일치** (실패) | **완전 일치** |
| 크롭 오프셋 · 크기 | ±4 px (경고) | — (입력이 이미 크롭) |
| `bbox` | ±8 px (경고) | ±1 px |
| `score` | ±2e-3 (경고) | ±2e-3 |
| VLM `종류` · `크기` | — (모듈에 2단 없음) | 완전 일치 |
| VLM `본것` 문장 | — | **비교 대상에서 제외** |

**게이트와 박스 수만 실패로 처리합니다.** 이 둘이 운영 동작이기 때문입니다.
좌표는 Pillow 버전에 따라 1~5 px 흔들리는 것을 실측했으므로(§10.3) 경고로 둡니다 —
경고까지 실패로 만들면 픽스처가 매번 깨지고, 매번 깨지는 테스트는 아무도 안 봅니다.

> **VLM 문장을 비교하지 마세요.** `do_sample=False` 라 greedy 이므로 같은 버전·같은 GPU 에서는
> 재현되지만, 토큰 단위 일치는 부동소수에 취약합니다.

### 7.4 검증 상태

| | |
| --- | --- |
| 배포 픽스처 생성 | ✅ 2026-08-06 · A100 80GB · transformers 5.14.1 |
| `--verify-fixture` 비교 로직 | ✅ 오프라인 7종 시험 통과 — 그대로 재생 시 오경보 0, 게이트 뒤집기·박스수 ±1·해시 불일치 **전부 검출**, 3px 드리프트는 통과·10px 는 경고 |
| 사람 라벨 일치 | 배포 경로 **19/20** (평가 경로 17/20) |
| 배포에서 실행 | 🔴 GPU 환경에서 1회 돌려 볼 것 (로컬엔 CUDA 없음) |

### 7.5 어디에 두나

| | 위치 | 이유 |
| --- | --- | --- |
| `golden_fixture*.json` | **git 레포** (프로젝트 루트) | 파일명·해시·박스 수치뿐 |
| 원본 이미지 20장 | **S3 `models/fixtures/rgb/`** | 데이터셋 라이선스 대상이라 커밋 불가 |
| `ext_requirements.lock.txt` | git 레포 | 이 픽스처를 만든 환경 (§2) |

---

## 8. 재현 (학습 없음)

### 8.1 평가셋

기존 라벨을 못 믿으므로 **시험지를 새로 만들었습니다**(§9.3).

| 항목 | 1차 | 2차 |
| --- | --- | --- |
| 프레임 | 150장 | 150장 (1차와 **겹침 0**) |
| **합계** | \- | **300장 · 점 684개 · 무결함 99장** |
| 추출 | 셀당 최대 2장 층화, split=`val` 고정 | seed 2026, 1차 파일명 제외 |
| 용도 | 운영점 결정 · 규칙 탐색 | 최종 검증 전용 |

**셀당 최대 2장인 이유**: 한 셀의 270프레임은 서로 닮아 상관 표본이 됩니다(유효 표본 수가 줍니다).

### 8.2 지표 · 대조군

| | |
| --- | --- |
| 주지표 | **점(point) recall** — 사람 점이 예측 박스 안에 들어가면 적중 |
| 프레임 지표 | Precision / Recall / F1 |
| **금지** | **IoU 안 씁니다** |

**IoU 를 폐기한 이유**: 기존 GT 는 결함 전체가 아니라 파편이라, 결함을 정확히 잡을수록 면적이 커져
IoU 가 **떨어집니다**. 점은 조각이 될 수 없고 크기 논쟁도 없습니다.

**순열 대조 필수**: 이미지 이름을 섞어 **남의 이미지 박스로 채점**합니다. 박스를 많이(또는 크게)
뿌리면 우연히 맞기 때문입니다.

| 지표 종류 | 판정 통계 |
| --- | --- |
| recall · 정확도 | **배율 ≥ 2** |
| F1 스윕 | 순열 p < 0.01 & F1 > p95 (순열 바닥이 기저율 ~0.5 이라 배율 상한 2.0) |
| 다중클래스 정확도 | **최빈 유형 베이스라인** 대비 |

🔴 **세 번째가 가장 비싼 교훈입니다.** 순열 대조를 통과해도 "가장 흔한 유형 찍기" 보다 못할 수
있습니다 — 실측: 검출기 태그 0.267 < 무조건-녹 **0.376**.

**지표와 폐기 규칙은 예측을 보기 전에 고정합니다.** 실제로 recall 0.738(순열 1.38×)과
recall 0.993(순열 1.24×)을 이 규칙으로 폐기했습니다.

### 8.3 노트북 실행 순서

| 목적 | 순서 |
| --- | --- |
| 리포트 JSON 생성 (운영) | `unsup §1` → `evalset §C` → `§M3` → `§Y` |
| 게이트 성능 재현 | `unsup §1` → `evalset §C` → `§C-run` |
| 2단 VLM 덤프 | `unsup §1` → `unsup §4` → `evalset §C` → `§E` → `§F` |
| 모델카드 재검증 | `modelcard_check §0→§1→§3→§4→§5→§7→§8→(§6)→§2→§9` |

⚠️ `unsup §4-run` 은 돌리지 마세요 — 셀 리포트 전용이며 **옛 프롬프트**를 씁니다.

---

## 9. 성능

### 9.1 ✅ 검증된 것

| 지표 | 값 | 대조 |
| --- | --- | --- |
| **프레임 게이트** | P 0.855 / **R 1.000** / F1 0.922 | **300장 · 배포 경로**, 라벨 0 · 학습 0 |
| **셀 게이트** | **P 1.000 / R 1.000** | 결함 12셀 · 정상 12셀 (§5.7) |
| **점 recall** | **0.818** | 순열 0.328 → **2.51×** ✅ |
| YOLO 동일 시험지 | 0.089 | 순열 0.043 → 2.07× ✅ → **9.2배 차이** |
| 정상/결함 박스 밀도 | 결함 13.0/장 vs 무결함 2.1/장 | **6.3×** (thr 0.12) |
| VLM 크기 ↔ 실측 면적 | rho **+0.297** | 순열 1000회 **p = 0.001** ✅ |
| 검출 지연 (1단) | **341~406 ms/장** | 배포 경로 406ms (크롭 포함) — §10 |
| **셀당 지연** | **12.2초** (30프레임) | 전량 272프레임은 110초 — §10 |
| VLM 지연 (2단) | **1.19~1.21 초/호출** | A100 80GB bf16, 2런 |
| 골든 픽스처 회귀 | ✅ 20장 전부 일치 | §7 |

**유형별 검출률** ("찾았는가" 이지 "이름을 맞혔는가" 가 아닙니다)

| 유형 | 사람 점 | YOLO | **OWLv2** |
| --- | --- | --- | --- |
| 녹·부식 | 82 | 0.024 | **0.890** |
| 긁힘·스크래치 | 30 | 0.000 | **0.867** |
| 들뜸 | 15 | 0.000 | **0.600** |
| 벗겨짐·박리 | 73 | 0.137 | 0.767 |
| 오염·이물질 | 99 | 0.141 | 0.808 |

> ⚠️ 이 표의 사람 점 합이 299 라 **1차 150장(점 302) 기준**입니다. 300장 재집계는 안 했습니다.
> 게이트·점 recall 수치는 300장 기준이 맞습니다.

### 9.2 ❌ 검증 실패 — 제품에 안 쓰는 것

| 시도 | 결과 | 판정 |
| --- | --- | --- |
| VLM 심각도 등급 | 결함 19.7% vs **무결함 20.2%** 가 4등급↑ | 분리력 0 |
| VLM 유형 (크롭만, v8) | 0.157 | 베이스라인 0.340 대비 **−0.183** |
| VLM 유형 (셀 전체+크롭, v9) | 0.115 · 긁힘 답으로 69% 붕괴 | **−0.225** (더 나쁨) |
| 검출기 태그 유형 | 0.183 / 0.267 | 베이스라인 이하 |
| 픽셀 색 규칙 유형 | 0.457 | +0.081 (문턱 +0.10 미달) |
| top-2 / top-3 | 0.619 / 0.748 | 베이스라인 0.581 / **0.762** 대비 +0.038 / **−0.014** |
| 예산 모드 recall 0.738 | 순열 1.38× | 폐기 |
| 질의어 교체 recall 0.993 | 순열 1.24×, 비대칭 6.4→4.4 | **박스가 커진 것** |
| 셀 위/아래 밴드 필터 | N≥12 에서 ON/OFF 동일, N≥8 에서 놓침 0→1 | 상방 없음 |
| VLM 신뢰도 점수 | 캘리브레이션 −0.477 | 내보내지 않음 |

> 🔴 **유형 이름은 미해결입니다.** 네 갈래로 독립 확인했습니다 — VLM 크롭 · VLM 문맥 · 검출기 태그 ·
> 픽셀 색 규칙. **남은 구조적 가설이 없습니다.**
>
> **⚠️ 이것이 게이트에 주는 영향은 0 입니다.** 게이트는 태그를 안 쓰고 박스 개수만 셉니다.

**v9 가 실패한 메커니즘 (기록)**: 셀 전체는 **길쭉한 원통에 세로 결·랩 주름**이 지배적인 그림이라
"선 = 긁힘" 으로 프라이밍됐습니다. **문맥을 줬더니 문맥이 답을 오염시켰습니다.**

### 9.3 왜 지도학습(YOLO)을 안 쓰나

| 근거 | 실측 |
| --- | --- |
| '무결함' 이라던 셀 515·581 | 오염 실재 |
| 표면이 가장 고른 셀 10개 전수 확인 | **10개 전부 결함** = 정상 클래스 부재 |
| conf≥0.10 검출의 라벨 미대응 | 46.4% |
| GT 가 결함 전체가 아니라 조각 | 면적비 p50 **112배** |
| 모델 레버 7개 결과 | 전부 0.12~0.22 수렴 |

→ **정답이 필요 없는 모델**로 전환했습니다. 질의어(텍스트)가 라벨의 역할을 대신합니다.

---

## 10. 배포 — 모듈 · 인터페이스 · 지연

### 10.1 배포 산출물은 `ext_infer.py` **한 파일**입니다

노트북은 **평가·실험용**입니다. 배포에는 쓰지 않습니다(평가 노트북 32셀 중 배포 경로는 6개뿐이었습니다).

```python
from ext_infer import ExtInspector, CONTRACT

insp = ExtInspector()                          # 모델 1회 로드 (지연 로딩)
out  = insp.infer_cell('0041', frame_paths)    # 셀 하나

out['셀_판정']            # '결함' / '정상'  ← 계약 REJECT / PASS
out['flag율']             # 0.0~1.0
out['셀_판정_근거']        # {'규칙': 'flag율 ≥ 70%', 'flag': '27/30', ...}
out['프레임'][i]['판정']
out['프레임'][i]['결함'][j]['위치']['bbox']    # ★원본 좌표 [x1,y1,x2,y2]
```

```bash
python ext_infer.py --cell 0041 --glob "frames/0041_*.jpg" --out result.json
python ext_infer.py --notes        # 배포 전 확인사항 (DEPLOY_NOTES)
python ext_infer.py --selftest     # 모델 없이 좌표 변환 · 계약서 스키마 검증
python ext_infer.py --verify-fixture \
    --fixture golden_fixture_deploy.json --images <원본 20장 폴더>   # 회귀 (§7.2)
```

| 공개 API | 역할 |
| --- | --- |
| `ExtInspector.load()` | 모델 로드(1회). 이후 호출은 재사용 |
| `ExtInspector.infer_cell(cell_id, images, names=None)` | **셀 판정 + 프레임별 결과** (권장 진입점) |
| `ExtInspector.infer_frames(images, names=None)` | 프레임별만 |
| `Config` | 모든 상수 (임계 · 크롭 · revision · 배치). `frozen dataclass` |
| `CONTRACT` | 리포트 LLM 계약서 (필드별 신뢰 등급) |
| `DEPLOY_NOTES` | 배포 주의사항 원문 |
| `verify_fixture(fixture, images_dir)` | **골든 픽스처 회귀** — 회귀 시 `exit 1` (§7.2) |

### 10.2 🔴 입력 규격 — **원본 JPEG 을 재인코딩하지 마세요**

**JPEG 압축 세대가 검출 수를 크게 바꿉니다** (실측):

| 입력 | 박스/장 |
| --- | --- |
| 원본 JPEG 그대로 (1세대) | **33.1** |
| 크롭 후 JPEG 재저장 (2세대) | 26.4 (−20%) |
| 평가셋 크롭본 (2세대) | 21.6 (−35%) |

재압축은 미세 질감을 뭉갭니다. 그 질감이 저점수 검출을 만들므로 **박스가 줄어듭니다.**
검증: 배포 크롭을 품질 92 로 재저장했더니 평가셋 수치에 붙었습니다(차이 53% → 22%).

> **계약**: 백엔드는 **카메라 원본 JPEG 바이트를 그대로** 넘깁니다.
> 리사이즈 · 품질 변경 · 재저장 · 썸네일 경유 **금지**. 하면 `thr_gate` 를 다시 잡아야 합니다.
> 파이프라인이 크롭을 **메모리에서만** 다루는 이유이기도 합니다(디스크 경유 = 재인코딩 위험).

| 항목 | 값 |
| --- | --- |
| 입력 형식 | 파일 경로 · **bytes** · PIL Image (셋 다 가능) |
| 입력 크기 | **원본** (실측 1920×1080). 크롭은 내부에서 수행 |
| 디스크 I/O | **없음** — 추론 경로에 파일 쓰기 0건 |
| 메모리 | 배치 단위 스트리밍. 셀 270장을 한 번에 올리면 약 1.6 GB 라 **`cfg.batch`(16)장씩만** 상주 |

### 10.3 출력 좌표계 — 항상 **원본 기준**

```
원본  →  crop_cell(orig) → (크롭, 오프셋 ox,oy)
      →  detect(크롭)     → bbox = 크롭 좌표
      →  출력            → [x0+ox, y0+oy, x1+ox, y1+oy]
```

`bbox_크롭` · `크롭_오프셋` 도 같이 나가므로 백엔드가 언제든 역산해 검증할 수 있습니다.
`ext_infer.py --selftest` 가 왕복 변환을 20회 검증합니다.

### 10.4 지연 — 셀 서브샘플링으로 9배 줄였습니다

**프레임 단위** (A100 80GB · 배포 경로 · 1920×1080 입력)

| 단계 | ms/장 | 비중 |
| --- | --- | --- |
| 디코드 + 셀 크롭 (CPU) | 29 | 7.6% |
| **검출 (GPU)** | **353** | **92.4%** |
| 합계 | 406 | |

**GPU 가 92% 입니다.** CPU 최적화는 의미가 없습니다.
배치를 16배 키워도 7% 밖에 안 빨라집니다 = 런치 오버헤드가 아니라 **연산 자체가 포화**입니다
(정사각 패딩 + **질의 35개를 매 이미지마다 인코딩**).

**셀 단위**

| 방식 | 셀당 | 목표(4초) 대비 |
| --- | --- | --- |
| 전량 272프레임 | 110.4초 | 27배 |
| **30프레임 균등 추출** ← 확정 | **12.2초** | **3배** |
| 20프레임 | 8.1초 | 2배 |
| 10프레임 | 4.1초 | **1배** (recall 0.993) |

**판정 규칙을 바꾼 것만으로 27배 → 3배가 됐습니다.** 프리필터도 FP16 도 쓰지 않았습니다.
근거는 §5.7 — 결함은 지속적이라 30장만 봐도 flag율이 안정적으로 나옵니다.

**남은 최적화 (미적용)**

| | 방법 | 기대 | 재현율 영향 |
| --- | --- | --- | --- |
| 1 | **질의 임베딩 캐시** — 질의 35개가 고정인데 매 이미지 재인코딩 | ×0.7 → **8.5초** | **없음** (순수 최적화) |
| 2 | FP16 · TensorRT | ×0.25 (통상) → 3초급 | 없음 (수치 검증 필요) |
| 3 | K 를 30 → 10 | **4.1초** | 셀 recall 1.000 → **0.993** |

**1번을 먼저 하세요** — 정확도 손실이 없습니다. 그래도 부족하면 3번이 **제품 결정**입니다.

### 10.5 배포 체크리스트

| | 항목 | 상태 |
| --- | --- | --- |
| 1 | `ext_infer.py --selftest` | ✅ 좌표 20/20 · 계약서 스키마 |
| 2 | 배포 경로 성능 실측 | ✅ §6.1 · §6.2 |
| 3 | 모델 `revision` 고정 | ✅ `Config` 에 박힘 (§1) |
| 4 | 라이브러리 버전 고정 | ✅ `transformers >=5.13.1,<6` (§2) · `ext_requirements.lock.txt` |
| 5 | 출력 좌표계 = 원본 | ✅ §10.3 |
| 6 | 골든 픽스처 회귀 | ✅ 배포 픽스처 20장 + `--verify-fixture` (§7) · 🔴 GPU 에서 1회 실행 필요 |
| 7 | **입력 재인코딩 금지 합의** | 🔴 **백엔드 확인 필요** (§10.2) |
| 8 | 지연 목표 | ⚠️ 12.2초 vs 4초 (§10.4) |
| 9 | 계약 enum 확장 | 🔴 팀 협의 (§5.3) |

---

## 11. 한계 · 금지사항

### 11.1 한계

- 평가 표본 **300장**. 더 늘리면 좋습니다
- **정답을 찍은 사람과 성능을 판단하는 사람이 같습니다** → 팀 교차 라벨링으로 보강 예정
- **결함 유형 분류 미해결**(§9.2). 게이트에는 영향 없으나 리포트 품질을 제한합니다
- **심각도(등급)에 정답 라벨 자체가 없습니다.** GT 는 결함/무결함 점 라벨뿐이라 채점 방법이 없습니다
- 크기는 **큼/작음 2단계**만 유효 (VLM 이 4단계 중 A 21% · C 74% 만 실사용)
- 🔴 **1단 지연이 배포 요구를 크게 초과합니다**(§10)
- 점 recall 은 IoU 기준 옛 수치와 **다른 잣대**입니다. 나란히 놓지 마세요 —
  같은 시험지의 YOLO 0.089 하고만 비교 가능합니다

### 11.2 금지사항

| | |
| --- | --- |
| 🔴 | **외부 API 호출 금지** — 배터리 도메인 제약. 두 모델 다 로컬 GPU |
| 🔴 | **매니페스트 라벨 사용 금지** — 맞는 게 거의 없습니다(§9.3) |
| 🔴 | **유형 이름을 확정으로 리포트하지 말 것** — 검증 미통과 |
| 🔴 | **유형 신뢰도 점수를 외부로 내보내지 말 것** — 캘리브레이션 붕괴 |
| 🔴 | **`정상:금속캡` 을 `DROP_TAGS` 에 넣지 말 것** — 진짜 결함을 더 많이 잃습니다 |
| 🔴 | **`PIL.thumbnail()` 쓰지 말 것** — 확대를 안 합니다. `resize` |
| 🔴 | **프롬프트에 예시·기준표·특징 목록을 넣지 말 것** — 그대로 복사됩니다 |
| 🔴 | **예산(장당 N박스) 모드로 평가하지 말 것** — 정상 프레임에도 N박스를 강제해 신호를 가립니다 |

---

## 12. `confidence` 산식 (요청 §7 답변)

계약 §12.1 은 최상위 `confidence` 를 `REJECT` = 결함 판정 신뢰도, `PASS`(후보 있음) =
`1 - (임계값 미만 최고 결함 점수)`, `PASS`(후보 없음) = `1.0` 으로 정의합니다.

**EXT 는 이 값을 공짜로 낼 수 있습니다.** CT 와 달리 병합 단계가 없기 때문입니다.

### 판정을 결정하는 스칼라 = **8번째로 높은 박스 점수** (`s₈`)

게이트는 `점수 ≥ 0.10 인 박스가 8개 이상` 입니다. 이 조건은 **`s₈ ≥ 0.10` 과 동치**입니다.
즉 판정을 뒤집는 유일한 값이 `s₈` 이고, 이게 CT 의 "검출 confidence" 에 정확히 대응합니다.

| 계약 케이스 | 산식 | 의미 |
| --- | --- | --- |
| `REJECT` | `s₈` (≥ 0.10) | 게이트가 얼마나 여유 있게 발화했나 |
| `PASS` (후보 있음) | `1 − s₈` (s₈ < 0.10) | 임계 미만 최고 "판정 점수" |
| `PASS` (후보 없음) | `1.0` | 박스가 8개 미만이면 `s₈ = 0` → 자동으로 1.0 |

**세 케이스가 하나의 식으로 자연스럽게 떨어집니다.**

> **셀 단위 `confidence`** 는 `flag율` 을 그대로 쓰는 것이 자연스럽습니다 —
> 판정이 `flag율 ≥ 0.70` 이므로 `REJECT` 면 `flag율`, `PASS` 면 `1 − flag율` 입니다.
> 결함 셀은 0.80~1.00, 정상 셀은 0.125~0.50 에 분포하므로 값의 의미도 살아 있습니다(§5.7).

### 왜 추가 비용이 없나

`topk = 50` 으로 **넓게 수집한 뒤 점수 임계로 사후 필터** 하는 구조입니다(§6). 즉 `s₈` 은
이미 계산된 값 중 8번째를 읽는 것이고, **재추론이 필요 없습니다.** CT 가 겪은 "conf 를 낮춰 한 번 더
돌려야 하나" 문제 자체가 없습니다 — 슬라이스 병합(NMS/NMM)이 없어 저점수 박스가 고점수 박스의
생존에 영향을 줄 수 없기 때문입니다.

⚠️ **단서 2개**

1. **`topk` 가 8 이상이어야 합니다.** 현재 50 이라 여유롭지만, 낮추면 `s₈` 자체가 안 나옵니다.
2. **위는 파이프라인 구조에서 나온 추론이지 별도 실측이 아닙니다.** 검증은 쌉니다 —
   `detect()` 를 `thr=0.0` 으로 한 번 돌려 `s₈` 을 뽑고, `s₈ ≥ 0.10` 판정이 §7 골든 픽스처의
   게이트 판정 20건과 전부 일치하는지 보면 확정입니다.

---

## 13. 남은 갭

| # | 항목 | 누가 | 심각도 | 비고 |
| --- | --- | --- | --- | --- |
| 1 | **6유형 → 계약 enum 매핑** (`CORROSION`·`PEELING` 추가?) | 팀 + 계약 | 🔴 | §5.3. **코드로 해결 불가** |
| 2 | **배포 지연 대책 결정** | 팀 | 🔴 | §10. 프리필터 채택 여부 + 채택 시 R 재측정 |
| 3 | **셀 판정 규칙** — PASS/REJECT vs 프레임 집계 | 팀 + 계약 | 🔴 | §5.7. 무결함 셀이 없어 셀 단위가 성립 안 함 |
| 4 | 배포 GPU 사양 (Qwen 7B bf16 ~16GB + OWLv2 수용?) | 인프라 | 🔴 | 측정은 A100 80GB |
| 5 | 크롭을 `ai-infer` 가 하는가, 크롭본을 받는가 | 팀 | ⚠️ | §3.1. 좌표 원복 책임 소재 |
| 6 | `source_commit` | 우리 | ⚠️ | 커밋 후 기입 |
| 7 | `ext_requirements.lock.txt` 를 레포에 커밋 | 우리 | ⚠️ | 값은 확보(§2.1) · 파일만 없음 |
| 8 | 유형별 검출률 300장 재집계 | 우리 | ⚠️ | §9.1 표가 1차 150장 기준 |
| 9 | YOLO 대조군의 `iou`·`max_det`·`half`·SAHI postprocess | 우리 | ⚠️ | §14 |

**✅ 이번 회차에 닫힌 것**: HF `revision` 고정 · 라이브러리 버전 실측 · 1단/2단 지연 실측 ·
골든 픽스처 20장 · 운영점 300장 재현 · `topk` 확정(50) · 출력 스키마 확정(§5.4) ·
VLM 정상 판별력 측정(§9.2 — 분리력 0 으로 확인, 그래서 안 내보냄).

---

## 14. 대조군 — YOLO 트랙 (보존)

**지우지 않습니다.** 같은 시험지의 대조군이고, 계약이 YOLO 를 전제하고 있어 되돌아갈 수도 있습니다.

| 항목 | 값 |
| --- | --- |
| 가중치 | `train_ext_crop_v5_idcap/best.pt` (점 recall 채점 대상) |
| 아키텍처 | `yolo11m` (detection) |
| 클래스 | `{0: Damaged, 1: Pollution}` |
| `imgsz` | 1280 |
| 구조 | 클래스별 2패스 — `Pollution` plain conf **0.30** / `Damaged` SAHI 640·0.2 conf **0.02** |
| 점 recall | **0.089** (thr 0.05, 순열 배율 2.07×) |

- ⚠️ **EXT 전체를 SAHI 로 돌리지 마세요** — plain 이 이미 R=1.0 이고 SAHI 는 오탐만 늘립니다.
- ⚠️ conf 0.30/0.02 와 "image-level F1 0.98" 은 **v3.4 계열(`train_ext_v34`) 기준**이고,
  점 recall 0.089 는 `crop_v5_idcap` 기준입니다. **다른 가중치·다른 시험지라 섞어 읽으면 안 됩니다.**
- ⚠️ F1 0.98 은 `Pollution` 이 캐리한 평균값입니다(클래스별 0.997 / 0.457).
- ❓ `iou` · `max_det` · `half` · SAHI `postprocess` 미기록. conf 0.02 는 sahi 자동전환 구간이라
  CT 에서 발견한 함정이 그대로 적용됩니다.

---

## 15. 관련 파일

| 파일 | 역할 |
| --- | --- |
| **`ext_infer.py`** | **배포 모듈 (단일 파일) · 런타임 값의 원본** — `Config` · `CONTRACT` · `DEPLOY_NOTES` |
| `golden_fixture_deploy.json` | **배포 회귀 픽스처** (원본 20장 · thr 0.10) — §7 |
| `golden_fixture.json` | 검출기 회귀 픽스처 (크롭 20장 · thr 0.08) — §7 |
| `ext_requirements.lock.txt` | 픽스처를 만든 환경 잠금 (torch · transformers · pillow …) |
| `ext.model_card.json` | 이 문서의 기계 판독 형태 (S3 배포본) |
| `EXT_SPEC.md` | **재현 스펙** — 코드 계약 · 질의어 전문 · `_contract.json` 전문 · 실행 순서 |
| `battery_ext_modelcard_check.ipynb` | 모델카드 검증 (11셀) — revision · 환경 · 지연 · 운영점 · 픽스처 |
| `battery_ext_unsup.ipynb` | §0 크롭본 / **§1 OWLv2** / **§4 Qwen 로드** (12셀) |
| `battery_ext_evalset.ipynb` | 평가셋 — §A 표본 / §B 라벨링 / §C 채점 / §Y 리포트 JSON (30셀) |
| `battery_ext_crop_retrain.ipynb` | 크롭 함수(§1) · YOLO 대조군 학습(§3) |
| `{OUT}/modelcard/` | `env.json` · `revisions.json` · `latency_*.json` · `operating_point.json` · `golden_fixture.json` |
