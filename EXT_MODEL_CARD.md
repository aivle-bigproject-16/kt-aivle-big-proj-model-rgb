# Model Card — EXT / RGB 외관 검사

원통형 리튬셀 표면 사진에서 결함을 찾아 위치·유형·심각도를 내는 파이프라인입니다.
`ai-infer` 가 이 문서만 보고 재현할 수 있도록 요청서 템플릿 순서로 작성했습니다.

- 기계가 읽을 형태 → [`ext.model_card.json`](ext.model_card.json)
- 근거 → `0804.md` §20 · `EXT_YOLO_vs_VLM_발표정리.md`

| | |
| --- | --- |
| 최종 갱신 | 2026-08-04 |
| 모달 | EXT / RGB (CT 는 `model-ct` 레포) |
| 구조 | **OWLv2(검출) → Qwen2.5-VL(해설) 2단.** 학습 0회 · 라벨 0장 |
| 외부 API | **0건** (전량 로컬 GPU) |

> ### 이 카드의 주어가 YOLO 가 아닌 이유
>
> 요청서 §3 은 RGB 를 YOLO(`Damaged`/`Pollution` → `CRACK`/`SPOT`)로 상정하고 쓰였습니다.
> 0804 에서 **사람이 직접 찍은 평가셋**으로 두 경로를 같은 시험지에 채점한 결과 YOLO 의 점 recall 은
> **0.089**, OWLv2 는 **0.818** (9.2배)이었고, 유형별로는 YOLO 가 **녹·부식 0.024 · 긁힘 0.000 ·
> 들뜸 0.000** 으로 해당 유형을 사실상 못 잡았습니다. 0804 §20.6 이 OWLv2 2단을
> **"확정된 운영 구조"** 로 정리했으므로 이 카드의 주어로 삼습니다.
> YOLO 는 **§7 대조군**으로 남깁니다(지우지 않습니다).
>
> ⚠️ 대신 계약과 안 맞는 것이 생겼습니다 — §5 `class_map` 과 §8 #2·#3 을 보세요.

---

## 1.1 식별

| 항목 | 값 |
| --- | --- |
| `weight_file` | **없음** — 학습된 가중치가 아니라 사전학습 모델 2개를 그대로 씁니다 |
| `detector_model_id` | `google/owlv2-large-patch14-ensemble` |
| `vlm_model_id` | `Qwen/Qwen2.5-VL-7B-Instruct` |
| `weight_sha256` | **개념 없음** → 아래 대응물 참고 |
| `train_run` | **없음** (학습 0회) |
| `labels_used` | **0** |
| `source_repo` | `aivle-bigproject-16/kt-aivle-big-proj-model-rgb` |
| `source_commit` | ❓ 미기입 |
| `trained_at` | 해당 없음 |

> ### `weight_sha256` 의 대응물 = **HF `revision` 고정**
>
> 가중치 파일이 없으니 해시로 검증할 대상도 없습니다. 대신 **HuggingFace 리포지토리는 갱신될 수
> 있습니다.** `from_pretrained(MODEL_ID)` 만 쓰면 어느 날 다른 가중치를 받아도 조용히 돌아갑니다.
>
> **배포 시 두 모델 다 `revision='<커밋 해시>'` 로 고정하세요.** 이것이 이 파이프라인에서
> `weight_sha256` 이 하는 일을 대신합니다. 현재 노트북은 고정하지 않고 있습니다(§8 #1).

---

## 1.2 라이브러리 버전 — ❌ 고정 안 되어 있습니다

노트북이 이렇게 깝니다:

```
!pip -q install -U transformers accelerate
!pip -q install -U "transformers>=4.49" accelerate qwen-vl-utils
```

`-U` = **매번 최신판**입니다. CT 가 `sahi` 기본값 때문에 겪은 문제가 여기서는 더 큽니다.

| 패키지 | 용도 | 현재 |
| --- | --- | --- |
| **`transformers`** | OWLv2 · Qwen2.5-VL 양쪽 | **미고정** (`>=4.49` 만) |
| `torch` | 런타임 | 미고정 |
| `accelerate` | `device_map='auto'` | 미고정 |
| `qwen-vl-utils` | Qwen 전처리 | 미고정 |
| `pillow` · `numpy` | 이미지 I/O | 미고정 |

> ### ⚠️ `transformers` 는 이미 한 번 코드를 죽였습니다
>
> 신버전에는 `post_process_object_detection` 이 없어 폴백을 마련한 코드가
> **폴백을 쓰기도 전에** `AttributeError` 로 터졌습니다(0804 §11 #1).
> 지금은 지연 평가로 둘 중 있는 쪽을 고릅니다:
>
> ```python
> _POST = (getattr(proc_d, 'post_process_grounded_object_detection', None)
>          or getattr(proc_d, 'post_process_object_detection', None))
> assert _POST, '★후처리 API를 못 찾음'
> ```
>
> **더 위험한 건 조용한 쪽입니다.** 후처리 구현이 바뀌어 점수 스케일이 달라지면
> §1.4 의 `thr 0.08` · `thr 0.12` 운영점이 **에러 없이 무효**가 됩니다.
> 채워야 할 것 — 그 파이프라인을 돌린 런타임에서 `pip freeze > ext_requirements.lock.txt`.

**GPU**: 노트북은 A100 40GB 기준입니다. Qwen2.5-VL-7B 가 bf16 로 ~16GB, OWLv2-large 가 별도.
배포 GPU 가 T4/L4 면 **들어가는지부터** 확인해야 합니다(§8 #3).

---

## 1.3 입력 전처리

| 항목 | 값 |
| --- | --- |
| 원본 규격 | `1920 x 1080` · `RGB_cell_cylindrical` · 759 ID 전부 동일 |
| `color_space` | `RGB` |
| 셀당 프레임 | ~270 (360도 회전 촬영) |
| **셀 크롭** | **있습니다** — 아래 |
| 정사각 패딩 | **필수** — 아래 |
| `imgsz` | 해당 없음 — OWLv2 프로세서가 처리 |
| `TILES` | `1` (크롭 통짜) |

### ① 셀 크롭 — 요청서 §3 "RGB 도 ROI 전처리가 있는가" 에 대한 답: 있습니다

예전 문서의 "EXT 는 crop 이 없어 이미지 = ROI = 전체 프레임" 은 **더 이상 맞지 않습니다.**
v4.2 부터 셀 크롭이 들어갔고, 학습·추론 공용 함수는 `battery_ext_crop_retrain.ipynb` §1 입니다.

| 파라미터 | 값 |
| --- | --- |
| 방식 | HSV 채도 기반 (배경=무채색 · 셀=유채색 전제) |
| `SAT_TH` | `40` |
| `DENS_TH` | `0.10` (행·열 마스크 밀도 — 흩어진 노이즈에 강건) |
| `PAD` | `0.10` (결함 보존 Damaged 100% / Pollution 99.7%) |
| `MIN_AREA` / `MAX_AREA` | `0.02` / `0.90` |
| 채도로 안 갈릴 때 | 밝기(V) 폴백 |
| 추정 실패 시 | **원본 전체 `(0,0,1,1)` 폴백** — 파이프라인이 멈추지 않음 |
| 크기 고정 | **아니오** (매 이미지 추정값) |

**리포트 좌표는 원본 기준이어야 합니다.** 크롭 좌표를 그대로 내보내면 프론트에서 박스가 어긋납니다.
같은 파일의 `to_orig_coords()` 로 되돌리세요(왕복 변환 자체 검증 있음).

### ② 정사각 패딩 — 생략하면 좌표가 통째로 어긋납니다

OWLv2 전처리가 이미지를 **정사각으로 패딩**합니다. 직사각을 그대로 넣으면 반환 좌표가
패딩된 캔버스 기준이라 **박스가 어긋납니다. 에러는 안 나고 숫자만 틀립니다.**

→ **우리가 먼저 정사각 패딩**해(원본을 좌상단, 나머지 검정) 내부 패딩을 무연산으로 만듭니다.

```python
S = max(sub.size)
sq = Image.new('RGB', (S, S), (0, 0, 0)); sq.paste(sub, (0, 0))
...
sizes = torch.tensor([[S, S]] * len(blk))   # target_sizes = 패딩 후 정사각 크기
```

---

## 1.4 추론 파라미터

```yaml
detector:
  model_id: google/owlv2-large-patch14-ensemble
  revision: <커밋 해시로 고정할 것>        # §1.1
  tiles: 1
  batch_size: 8
  drop_neg: true

stage1_gate:                               # 프레임 결함/정상 판정
  threshold: 0.08
  min_boxes: 8                             # 박스 N개 이상이면 결함

stage2_locate:                             # VLM 에 넘길 박스 선정
  threshold: 0.12

vlm:
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
  dtype: bfloat16
  mode: per_item                           # montage 아님
  do_sample: false                         # greedy
  max_new_tokens: 1600
```

### 2단으로 나눈 이유 — 임계 하나로 통일하면 안 됩니다

| 단 | 임계 | 역할 | 실측 |
| --- | --- | --- | --- |
| ① 게이트 | **0.08** · N≥8 | 결함 프레임을 **하나도 안 놓치기** | P 0.876 / **R 1.000** / F1 0.934 |
| ② 위치 특정 | **0.12** | 박스 위치가 맞아야 VLM 이 제대로 봄 | 점 recall **0.818** (순열 배율 2.13×) |

게이트는 **박스 개수만** 세므로 위치 정확도가 필요 없습니다. 그래서 0.08 의 낮은 순열 배율(1.35×)이
무해합니다. 위치가 필요한 ②만 0.12 를 씁니다. **역할 분리로 둘 다 최적입니다.**

### 질의어 = 이 파이프라인의 실질 "라벨"

`QUERY_MAP` 6유형 **23질의** + `NEG_MAP` 방해질의 4종 **10질의** = 총 33개를 한 번에 넣습니다.
OWLv2 는 박스마다 가장 잘 맞는 질의를 고르므로, 정상 요소에 이름을 주면 그 박스가 결함 질의를
훔쳐가지 않고 제 이름으로 붙습니다 = 걸러낼 수 있습니다.

```python
DROP_TAGS = {'정상:인쇄·각인', '정상:반사', '정상:테두리'}
```

> 🔴 **`정상:금속캡` 은 이름만 붙이고 버리지 않습니다.** 구조물 판정으로 지우면
> 오탐 5개 지우고 **진짜 결함 9개를 잃습니다**(순손실 −4). 캡 위의 부식은 결함입니다.
> `DROP_TAGS` 에 `'정상:금속캡'` 을 추가하지 마세요.

질의어 설계 규칙(OWLv2 는 CLIP 계열 텍스트 인코더): ① 영어 짧은 명사구 ② 추상어(`defect`·`anomaly`)는
안 먹힘 — 눈에 보이는 물체/현상으로 ③ 한 유형에 영어 표현 여러 개를 OR ④ **한국어 키는 평가셋
`TYPES` 와 같은 이름**(유형별 recall 을 내려면 어휘가 같아야 함. `assert` 로 강제).

### 라이브러리 기본값 / 미확정

| 파라미터 | 값 | 비고 |
| --- | --- | --- |
| `bs` (배치) | `8` | 노트북 값 |
| `TILES` | `1` | 작은 결함을 놓치면 3(세로 3등분). 미사용 |
| `TOPK` | `12` | ⚠️ **아래 불일치** |
| 후처리 API | `post_process_grounded_object_detection` 우선 | 없으면 구 이름으로 폴백 |
| `revision` | **미고정** | §1.1 |

> ### ⚠️ `TOPK` 운영값이 실측과 안 맞습니다
>
> 노트북 기본 `TOPK = 12`(장당 박스 상한)인데, §6 의 실측은 `thr 0.08` 에서 **결함 프레임 27.4 박스/장**
> 입니다. 12 로 잘렸다면 27.4 가 나올 수 없으므로 **채점은 `topk` 를 올린 상태에서 돈 것**입니다.
> 게이트가 N≥8 이라 12 로도 동작은 하지만, **§6 수치를 재현하려면 실제 쓴 `topk` 값이 필요합니다.**

---

## 1.5 출력 매핑

### 검출기 출력

```
(x1, y1, x2, y2, score, tag, query)     # 원본 크롭 좌표계 (타일 오프셋 복원 완료)
```

| 항목 | 값 |
| --- | --- |
| `bbox_format` | `xyxy` |
| `coordinate_reference` | 크롭 이미지. **원본 기준 리포트는 `to_orig_coords()` 필요** (§1.3) |
| `tag` | 한국어 유형 6종 또는 `정상:*` |

### 결함 유형 6종

`녹·부식` · `벗겨짐·박리` · `파손·찢김` · `긁힘·스크래치` · `들뜸` · `오염·이물질` (+ VLM 은 `기타` 허용)

### 🔴 `class_map` — **계약 enum 과 안 맞습니다**

| | |
| --- | --- |
| 계약 §6.5 RGB enum | `CRACK` · `SPOT` (2종) |
| 이 파이프라인 | 위 6종 |
| `class_map` | ❓ **규칙 없음** |

`파손·찢김` → `CRACK`, `오염·이물질` → `SPOT` 까지는 자연스럽지만 **`녹·부식`(사람 점 82개로 최다) ·
`긁힘·스크래치` · `벗겨짐·박리` · `들뜸` 이 갈 곳이 없습니다.** 계약 전체 enum 에 `SWELLING` 이 있어
`들뜸` 은 거기로 볼 여지가 있으나 RGB 는 2종만 쓰기로 돼 있습니다.

**선택지는 둘입니다** — ① 6종을 2종으로 뭉갠다(정보 손실. 사용자가 지목한 "녹을 못 잡는다"를 다시
못 보게 됨) ② 계약 enum 을 늘린다. **계약 쪽 결정이 필요합니다**(§8 #2).

### VLM 출력 (per_item, 크롭 1장당 1회)

| 필드 | 값 |
| --- | --- |
| `관찰` | 보이는 것 묘사 (**판정보다 먼저**) |
| `판정` | `결함` / `정상` |
| `종류` | 결함이면 위 6종+`기타`, 정상이면 `각인·인쇄` / `반사·얼룩` / `정상 표면` |
| `위치` | `금속캡` / `상단` / `몸통` / `하단` / `불명` |
| `등급` | `1`~`5` 정수 |
| `근거` | 한 문장 |

> **`관찰` 필드가 판정보다 앞에 오는 것은 의도된 순서입니다.** 자기회귀라 먼저 나온 토큰이 뒤를
> 구속하므로, 판단 전에 본 것을 묘사시켜 모델을 사진에 붙들어 맵니다. 순서를 바꾸지 마세요(§4).

**5등급 (리튬셀 안전 기준)** — 3단계(상/중/하)는 실측에서 전부 한 값으로 뭉쳐 변별력이 0이었습니다.
분기점은 크기가 아니라 **금속이 드러났는가 / 안전변이 성한가** 입니다.

| 등급 | 기준 | 처분 |
| --- | --- | --- |
| 5 치명 | 금속 노출(외피 관통·찢김), 캡·안전변 변형·파손, 깊게 눌림 | 폐기 |
| 4 중대 | 외피 벗겨짐·박리 진행 중, 넓은 부식(면적 1/4 이상) | 폐기 후보 |
| 3 보통 | 뚜렷한 국소 부식·변색·들뜸. **열자마자 눈에 띄는 크기** | 재검사 |
| 2 경미 | 자세히 봐야 보이는 얕은 긁힘·옅은 얼룩 | 기록만 |
| 1 무시 | 머리카락처럼 가는 선, 확대해야 겨우 보이는 점 | 통과 |

---

## 2. 아직 없는 것 (요청서 §2.2 대응)

1. **라이브러리 버전 고정** (§1.2) — `ext_requirements.lock.txt` 없음
2. **HF `revision` 고정** (§1.1) — `weight_sha256` 대응물
3. **`class_map`** (§1.5) — 6종 → 계약 enum
4. **`TOPK` 운영값** (§1.4) — 12 인데 실측은 27.4 박스/장
5. **지연 측정** — 셀당 270프레임. **배포 가능 여부가 여기서 갈립니다**
6. **골든 픽스처** (§5) — 0건
7. **출력 스키마 샘플** — CT 의 `ct_output_schema.sample.json` 대응물 없음

---

## 3. 짚어 둘 함정 (요청서 §2.3 대응)

### 3.1 🔴 셀 단위 `PASS`/`REJECT` 가 성립하지 않습니다

0804 §15: 표면이 가장 고른 셀 10개를 뽑아 육안 확인했더니 **10개 전부 결함**이었습니다.
매니페스트에서 '무결함' 으로 표시된 비-산업 셀 2개(515·581)도 **둘 다 오염이 실재**했습니다(§13).

**이 데이터에 무결함 셀이 사실상 없습니다.** 계약 §12.1 의 셀 단위 PASS/REJECT 는 분모가 없습니다.
0804 §18 #5 는 **등급 기반(폐기 / 재검사 / 통과)** 으로 재설계할 것을 제안합니다 — §1.5 의 5등급이
이미 그 처분을 담고 있습니다. **계약 쪽 논의가 필요합니다.**

단, **프레임 단위로는 사람이 검증한 무결함 51장이 있어** 게이트 P/R 을 잴 수 있습니다(§6).

### 3.2 🔴 예산(장당 N박스) 모드로 재면 신호가 가려집니다

처음엔 예산으로 재서 recall 0.738@10박스가 나왔는데 **순열 배율 1.38× 로 폐기**했습니다.
예산은 정상 프레임에도 N박스를 **강제**하므로 "정상에서 조용해지는가"를 측정 불가로 만듭니다
(무결함 오탐이 정확히 `예산×51`). **반드시 점수 임계로 스윕하세요.**

### 3.3 🔴 프롬프트의 예시 JSON = few-shot 라벨

출력 27개가 **필드 4개 전부 프롬프트 예시값과 일치**한 적이 있습니다. 금속캡이 아예 없는 칸까지
전부 `@금속캡` 이었습니다. VLM 이 이미지를 보는 게 아니라 적어준 답을 채우고 있었습니다.

→ 스키마를 `<플레이스홀더>` 로 두고 *"예시가 아니라 빈칸이다"* 를 명시했습니다.
**프롬프트에 그럴듯한 값을 예시로 넣지 마세요.** 70% 이상이 같은 조합이면 경고가 뜹니다.

### 3.4 몽타주(SoM 번호 참조)는 실패했습니다 — `per_item` 이 기본입니다

작은 VLM 이 번호↔칸 매핑에 약해, 캡이 없는 칸을 "금속캡 각인" 이라 하고 근거 문장이 9칸에 글자까지
동일했습니다(전체 인상 하나를 번호에 임의 배분). **`VLM_MODE='per_item'`** 으로 칸당 따로 물으면
매핑 문제가 원천적으로 없습니다. 비용은 셀당 ~9회 호출(~20초). 몽타주는 검증용으로만 남겼습니다.

부수 함정: **`PIL.thumbnail()` 은 확대를 안 합니다.** 63px 크롭이 320px 칸 구석에 붙어 흰 여백이
96% 가 된 적이 있습니다 → `MIN_CROP = 128` + `resize`.

---

## 4. 성능

시험지: `MyDrive/ext_evalset/labels_me.json` — **150장(결함 99 · 무결함 51) · 사람이 찍은 점 302개.**
지표: **점 recall**(예측 박스 안에 사람 점이 들어가면 적중). 대조군: **순열**(남의 박스로 채점),
사전 확정 규칙 **배율 < 2 면 폐기**.

### 임계 스윕

| thr | 점 recall | 순열 | 배율 | 결함99 박스/장 | 정상51 박스/장 | 비대칭 |
| --- | --- | --- | --- | --- | --- | --- |
| 0.08 | 0.954 | 0.705 | 1.35× ❌ | 27.4 | 5.9 | 4.6× |
| **0.12** | **0.818** | 0.384 | **2.13× ✅** | 13.0 | 2.1 | 6.3× |
| 0.18 | 0.507 | 0.132 | 3.84× ✅ | 5.3 | 0.53 | 10.1× |
| 0.25 | 0.291 | 0.056 | 5.20× ✅ | 2.3 | 0.18 | 13.2× |

### 프레임 게이트

| 설정 | P | R | F1 |
| --- | --- | --- | --- |
| **OWLv2 thr0.08 · N≥8** | 0.876 | **1.000** | **0.934** |
| OWLv2 thr0.12 · N≥5 | 0.957 | 0.909 | 0.933 |
| OWLv2 thr0.18 · N≥2 | 0.976 | 0.828 | 0.896 |

**라벨 0장 · 학습 0회로 R = 1.000** — 결함 프레임을 하나도 안 놓칩니다.

### 유형별

| 유형 | 사람 점 | OWLv2 (thr 0.12) | YOLO (thr 0.05) |
| --- | --- | --- | --- |
| 녹·부식 | 82 | **0.890** | 0.024 |
| 긁힘·스크래치 | 30 | 0.867 | **0.000** |
| 들뜸 | 15 | 0.600 | **0.000** |
| 벗겨짐·박리 | 73 | 0.767 | 0.137 |
| 오염·이물질 | 99 | 0.808 | 0.141 |

### 캐비엇

- 표본 150장으로 **작습니다.** 확장은 평가셋 `§A` 의 `N_SAMPLE` 만 올리면 됩니다.
- **정답을 찍은 사람과 성능을 판단하는 사람이 같습니다.** 팀 교차 라벨링(`WORKER`/`SHARD`)으로 보강 필요.
- 점 recall 은 IoU 0.1 기준 `0.217` 같은 옛 수치와 **다른 잣대**입니다. 나란히 놓지 마세요.
  같은 시험지의 YOLO 0.089 하고만 비교 가능합니다.
- **정상 51장 박스도 갈립니다** — 실제 2.1/장 vs 순열 7.4/장. 셔플하면 정상에 쏟아붓습니다
  = 진짜 검출기라는 증거이자 **이 프로젝트 최초의 정상/결함 분리 증거**입니다.

---

## 5. 골든 픽스처 — **0건**

요청서 §5.2 규격: 모델당 20~30장, PASS·REJECT 혼합, 실제 추론에 넣는 것과 완전히 같은 형태,
학습 노트북을 그대로 돌려 나온 출력 JSON.

**재료는 이미 있습니다** — `MyDrive/ext_evalset/images/` 150장(결함 99 · 정상 51) + `labels_me.json`.
그중 20~30장을 골라 `detect()` 출력을 굳히면 됩니다.

### CT 픽스처와 다르게 잡아야 할 것 2개

**① 검출기 허용오차를 CT(±0.01)보다 넓게.** OWLv2 점수는 `transformers` 후처리 구현에 민감할 수
있습니다. 다만 **게이트 판정(박스 개수 N≥8)과 verdict 는 완전 일치**로 두세요 — 거기가 제품 동작입니다.

**② VLM 은 구조화 필드만 비교.** `do_sample=False` 라 greedy 디코딩이므로 같은 버전·같은 GPU 에서는
재현되지만, 토큰 단위 일치는 부동소수에 취약합니다.
→ `판정` · `종류` · `등급` 만 비교하고 **`관찰`·`근거` 문장은 비교 대상에서 빼세요.**
문장을 문자열 비교하면 픽스처가 매번 깨져 아무도 안 보게 됩니다.

---

## 6. `ext.model_card.json`

[`ext.model_card.json`](ext.model_card.json) 참고. 요청서 §6 템플릿 뼈대를 따르되
가중치 파일이 없는 구조라 `identity` 는 모델 ID + `revision` 으로, `inference` 는 2단으로 채웠습니다.

---

## 7. 대조군 — YOLO 트랙 (보존)

**지우지 않습니다.** 같은 시험지의 대조군이고, 계약이 YOLO 를 전제하고 있어 되돌아갈 수도 있습니다.

| 항목 | 값 |
| --- | --- |
| 가중치 | `train_ext_crop_v5_idcap/best.pt` (점 recall 채점 대상) |
| 아키텍처 | `yolo11m` (detection) |
| 클래스 | `{0: Damaged, 1: Pollution}` |
| `imgsz` | 1280 |
| 구조 | 클래스별 2패스 — `Pollution` plain conf **0.30** / `Damaged` SAHI 640·0.2 conf **0.02** |
| 점 recall | **0.089** (thr 0.05, 배율 2.07×) |

- ⚠️ **EXT 전체를 SAHI 로 돌리지 마세요** — plain 이 이미 R=1.0 이고 SAHI 는 오탐만 늘립니다(0709 §6.1).
- ⚠️ 위 conf 0.30/0.02 와 "image-level F1 0.98" 은 **v3.4 계열(`train_ext_v34`) 기준으로 보입니다.**
  점 recall 0.089 는 `crop_v5_idcap` 기준입니다. **다른 가중치·다른 시험지라 섞어 읽으면 안 됩니다.**
- ⚠️ F1 0.98 은 `Pollution` 이 캐리한 평균값입니다(클래스별 0.997 / 0.457).
- ❓ `iou` · `max_det` · `half` · SAHI `postprocess` 미기록. conf 0.02 는 sahi 자동전환 구간이라
  CT 에서 발견한 함정이 그대로 적용됩니다.

---

## 8. 없는 정보 — 채워야 할 목록

| # | 항목 | 누가 | 비고 |
| --- | --- | --- | --- |
| 1 | `ext_requirements.lock.txt` + HF `revision` 고정 | 우리 | 🔴 `transformers` 가 운영점을 조용히 무효화할 수 있음 |
| 2 | **6유형 → 계약 enum 매핑** | 팀 + 계약 | 🔴 `CRACK`·`SPOT` 둘로는 안 담김 |
| 3 | **셀 판정 규칙** — PASS/REJECT vs 등급 기반 | 팀 + 계약 | 🔴 무결함 셀이 없어 PASS/REJECT 성립 안 함 |
| 4 | **OWLv2 지연** (셀당 270프레임) | 우리 | 🔴 배포 가능 여부가 여기서 갈림 |
| 5 | 배포 GPU 사양 (Qwen 7B bf16 ~16GB 수용?) | 인프라 | 🔴 |
| 6 | `TOPK` 운영값 (12 vs 실측 27.4) | 우리 | ⚠️ §6 재현에 필요 |
| 7 | 크롭을 `ai-infer` 가 하는가, 크롭본을 받는가 | 팀 | ⚠️ 좌표 원복 책임 소재 |
| 8 | 골든 픽스처 20~30장 | 우리 | ⚠️ 재료(150장)는 있음 |
| 9 | EXT 출력 스키마 샘플 | 우리 | ⚠️ CT 대응물 없음 |
| 10 | VLM 정상 판별력 (무결함 51장에 '정상' 비율) | 우리 | ⚠️ 0804 §20.8 #1, 미측정 |
| 11 | `source_commit` | 우리 | ⚠️ |
| 12 | YOLO 대조군의 `iou`·`max_det`·`half`·SAHI postprocess | 우리 | ⚠️ §7 |

**결정 없이 지금 할 수 있는 것: 1 · 4 · 6 · 10 · 12.**

---

## 9. 관련 파일

| 파일 | 역할 |
| --- | --- |
| `ext.model_card.json` | 이 문서의 기계 판독 형태 |
| `battery_ext_unsup.ipynb` | OWLv2(§1) + Qwen2.5-VL(§4) 파이프라인 (11셀) |
| `battery_ext_evalset.ipynb` | 평가셋 — §A 표본 / §B 클릭 라벨링 / §C 채점 |
| `battery_ext_crop_retrain.ipynb` | 크롭 함수(§1) · YOLO v5 학습(§3) |
| `battery_ext_crop_eval.ipynb` | 에폭·conf 스윕 |
| `battery_ext_recheck.ipynb` | localization-aware 재검 |
| `EXT_YOLO_vs_VLM_발표정리.md` · `0804.md` | 실측 근거 |
