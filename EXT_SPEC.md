# EXT 외관 검사 — 재현 스펙

> [`EXT_MODEL_CARD.md`](EXT_MODEL_CARD.md) 의 부록입니다.
> 카드가 **"어떤 값으로 쓰는가"** 라면 이 문서는 **"그대로 다시 돌리려면 무엇이 필요한가"** 입니다.
> 값·성능·한계는 전부 카드에 있고 여기서 반복하지 않습니다.
>
> 실험 이력(시도하고 폐기한 것들)은 담지 않습니다. 다만 **폐기 결과가 금지사항의 근거인 경우**는
> 카드 §9.2 · §11.2 에 한 줄씩 붙여 두었습니다 — 근거 없는 금지는 다음 사람이 되돌립니다.

| | |
| --- | --- |
| 최종 갱신 | 2026-08-06 |
| 대상 | OWLv2(검출) → Qwen2.5-VL(해설) 2단 |
| 학습 | **없음** (사전학습 가중치 그대로) |
| 외부 API | **0건** |

---

## 1. 파이프라인

```
입력: 셀 1개를 360° 회전 촬영한 RGB 프레임 N장 (운영 시 셀당 ~270장)
  │
  ├─[①] OWLv2  검출 (open-vocabulary)
  │      텍스트 질의 35개 → bbox + score + tag
  │      · 게이트  thr 0.10 · 박스 8개 이상 → 프레임 = 결함
  │      · 위치    thr 0.12 · 상위 6박스 → 다음 단계로
  │
  ├─[②] Qwen2.5-VL-7B  해설 (로컬)   ※선택 — 판정에는 안 쓰인다
  │      결함 후보 크롭 → 종류 후보 · 크기(A~D) · 관찰 문장
  │
  ├─[③] 규칙 계산 (코드)
  │      구역(캡/몸통) · 셀면적비 · 크기 구분(큼/작음)
  │
  └─[④] JSON 산출 → 리포트 LLM 에 전달
         값 + 각 값의 검증 상태를 같이 넘긴다
```

| 단계 | 역할 | 판정에 쓰이나 |
| --- | --- | --- |
| ① 게이트 | 프레임 결함/정상 | ✅ **이것만** |
| ① 위치 | 결함 위치 특정 | 리포트용 |
| ② 해설 | 종류 후보 · 크기 | ❌ |
| ③ 기하 | 구역 · 면적비 | 리포트용 |

> 🔑 **게이트는 태그도 종류도 안 씁니다. 임계 이상 박스의 개수만 셉니다.**
> ②를 통째로 빼도 게이트 성능은 동일합니다.

---

## 2. ① OWLv2 — 코드 계약

### 2.1 로드

```python
MODEL_ID = 'google/owlv2-large-patch14-ensemble'
REVISION = '95e26936e865f87db1742128404b3c035d47d89d'   # 고정 필수

proc_d = Owlv2Processor.from_pretrained(MODEL_ID, revision=REVISION)
mdl_d  = Owlv2ForObjectDetection.from_pretrained(MODEL_ID, revision=REVISION).eval().cuda()

# 후처리 API 이름이 transformers 버전 간 다르다. 지연 평가로 있는 쪽을 고른다.
_POST = (getattr(proc_d, 'post_process_grounded_object_detection', None)
         or getattr(proc_d, 'post_process_object_detection', None))
assert _POST, '★후처리 API를 못 찾음'
```

정밀도 fp32(기본). 라이브러리 버전은 카드 §2.

### 2.2 전처리 — 정사각 패딩

```python
S  = max(sub.size)                       # 긴 변
sq = Image.new('RGB', (S, S), (0, 0, 0)) # 검정 캔버스
sq.paste(sub, (0, 0))                    # 좌상단 정렬
...
sizes = torch.tensor([[S, S]] * len(blk))   # target_sizes = 패딩 후 정사각 크기
```

OWLv2 전처리가 이미지를 정사각으로 패딩하므로, 우리가 먼저 패딩해 내부 패딩을 무연산으로 만듭니다.
**생략하면 반환 좌표가 패딩된 캔버스 기준이라 에러 없이 박스만 어긋납니다.**

### 2.3 질의어 — 전문

```python
QUERY_MAP = {
  '녹·부식':      ['rust', 'orange brown discoloration', 'brown stain on surface',
                   'oxidation on surface', 'reddish brown spot'],
  '벗겨짐·박리':  ['peeling plastic wrap', 'peeled off label',
                   'exposed metal under wrapper', 'wrapper coming off',
                   'peeling film edge'],
  '파손·찢김':    ['tear in plastic film', 'crack on surface', 'puncture hole', 'dent'],
  '긁힘·스크래치': ['scratch', 'scratch mark', 'scratched metal surface', 'scuff mark'],
  '들뜸':         ['air bubble under film', 'wrinkled plastic wrap', 'lifted film edge'],
  '오염·이물질':  ['dirt stain', 'smudge', 'white residue', 'foreign particle on surface'],
}

NEG_MAP = {   # 방해 질의 — 정상 요소에 이름을 줘서 결함 질의를 훔쳐가지 않게 한다
  '정상:인쇄·각인': ['printed text', 'printed letters on label', 'engraved serial number'],
  '정상:반사':      ['light reflection', 'specular highlight'],
  '정상:금속캡':    ['metal cap', 'battery terminal'],
  '정상:테두리':    ['bottom edge of cylinder', 'rim of metal can', 'silhouette edge'],
}
DROP_TAGS = {'정상:인쇄·각인', '정상:반사', '정상:테두리'}   # 금속캡은 넣지 말 것
```

**총 35개** (결함 25 + 정상 10). 이 수가 지연에 직접 영향합니다(카드 §10).

설계 규칙(OWLv2 는 CLIP 계열 텍스트 인코더):

1. 영어 · 짧은 명사구. 문장·형용사 나열은 신호가 희석된다
2. 추상어(`defect`, `anomaly`)는 안 먹는다 — 눈에 보이는 물체·현상으로
3. 한 유형에 표현 여러 개를 붙여 OR 로 잡는다
4. **한국어 키 = 평가셋 라벨 유형과 같은 이름** (유형별 recall 을 내려면 어휘가 같아야 함)

질의어를 바꾸는 것은 **라벨을 바꾸는 것**입니다. 재학습이 필요 없는 대신, 바꾸면 유형별 수치를
다시 재야 합니다(게이트는 영향이 없다는 것이 실측입니다).

### 2.4 인터페이스

```python
detect(paths, thr, topk, tiles=1, bs=16, drop_neg=True)
  → {path: [(x1, y1, x2, y2, score, tag, query), ...]}   # 원본 크롭 좌표계, score 내림차순
```

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `x1,y1,x2,y2` | float | 크롭 이미지 픽셀 좌표 (타일 오프셋 복원 완료) |
| `score` | float | 질의 매칭 점수. **질의 간 스케일이 다름** |
| `tag` | str | 한국어 유형 또는 `정상:...` |
| `query` | str | 이긴 영어 질의 (진단용) |

전역 argmax 를 하므로 `tag` 는 "가장 잘 맞는 결함"이 아니라 "가장 점수가 잘 나오는 질의"입니다.
게이트는 개수만 세므로 무관하지만 **`tag` 를 유형으로 신뢰하면 안 됩니다.**

### 2.5 운영 파라미터

| 용도 | 임계 | 규칙 |
| --- | --- | --- |
| 게이트(판정) | `thr = 0.10` | 박스 **8개 이상** → 결함 |
| 위치(리포트) | `thr = 0.12` | 점수 상위 **6개** |
| `topk` (내부) | `50` | 사후 필터용 넓게 수집 |
| 배치 | `16` | 실측 최적 |
| 타일 | `TILES = 1` | 통짜. 작은 결함을 놓치면 3(세로 3등분)이 업그레이드 경로 |
| 밴드 필터 | 사용 안 함 | |

> 🔴 **`thr` 은 입력 경로와 짝입니다.** `0.10` 은 **배포 경로**(원본 → 셀 크롭, JPEG 1세대) 값입니다.
> 이미 잘린 크롭(JPEG 2세대)을 넣으면 박스가 적게 나와 `0.08` 이 맞습니다 — 평가 노트북이 그 경로입니다.
> **경로를 바꾸면 임계를 다시 잡아야 합니다.** 근거는 카드 §6.2 · §10.2.

성능·운영점 선택 근거는 카드 §6.

---

## 3. ② Qwen2.5-VL — 코드 계약

> 판정에는 쓰이지 않습니다. 리포트 재료만 만듭니다.

### 3.1 로드

```python
MODEL_ID = 'Qwen/Qwen2.5-VL-7B-Instruct'
REVISION = 'cc594898137f460bfe9f0759e9844b3ce807cfb5'   # 고정 필수
# bf16 (~16GB) · device_map='auto' · do_sample=False · max_new_tokens=120
```

### 3.2 입력 패널

```python
MARGIN, PANEL = 0.35, 336     # 박스 최대변의 35% 여유 · 336px 확대
CAP_ZONE      = 0.22          # 셀 높이 상단 22% = 금속캡 구역
```

`[ 기준 패치 | 확대 크롭 ]` 을 한 장으로 붙여 넣습니다.

| 구역 | 왼쪽(기준) |
| --- | --- |
| 몸통 | 같은 셀에서 박스가 가장 먼 곳을 같은 크기로 |
| 금속캡 | 사람이 확인한 **정상 캡 갤러리 16장** (셀이 달라도 같은 금속이라 셀 간 비교 성립) |

🔴 **`PIL.thumbnail()` 은 확대를 안 합니다.** 작은 크롭이 336px 칸 구석에 붙고 나머지가 흰 여백이
되어 VLM 이 사실상 빈 칸을 봅니다. **`resize` 를 쓰세요.**

### 3.3 프롬프트 v8 — 전문

```
왼쪽은 같은 셀의 다른 부위다. 오른쪽이 검사 대상이다.
왼쪽에는 없고 오른쪽에만 있는 것을 찾아라.

오른쪽에서 보이는 것을 고르라. 설명하지 말고 아래 세 줄만 답하라.

종류  0 없음  1 녹·부식  2 벗겨짐  3 찢김·파손  4 긁힘  5 들뜸  6 오염·이물질
크기  A 사진 절반 이상   B 사진의 1/4쯤   C 손톱만 함   D 점 하나

종류: (숫자)
크기: (알파벳)
본것: (한 문장)
```

**이 프롬프트를 고칠 때 지킬 것 두 가지** (근거는 카드 §4.3):

- **예시·기준표·특징 목록을 넣지 마세요.** 7B 는 눈에 띄는 텍스트를 그대로 출력에 복사합니다.
  틀을 하나 없애면 다음으로 눈에 띄는 텍스트를 베낍니다.
- **한국어를 생성시키지 마세요.** 번호·알파벳으로만 답하게 합니다.
- 모드는 `per_item` (크롭 1장당 1회). 몽타주(SoM 번호 참조)는 쓰지 않습니다.
- 셀 전체를 문맥으로 주지 마세요 — 세로 결·랩 주름이 "선 = 긁힘" 으로 프라이밍합니다.

### 3.4 출력 파싱

```python
종류: [0-6]  → KIND_ID = {0:'정상', 1:'녹·부식', 2:'벗겨짐·박리', 3:'파손·찢김',
                          4:'긁힘·스크래치', 5:'들뜸', 6:'오염·이물질'}
크기: [A-D]  → SIZE_G  = {'A':4, 'B':3, 'C':2, 'D':1}
본것: 자유 문장 (140자 절단)
```

정규식으로 읽습니다. **못 읽으면 종류 0(정상)으로 처리** — 모르는 걸 결함으로 치지 않습니다.

### 3.5 등급 — VLM 에 묻지 않고 코드가 계산

```python
KIND_UP = {2, 3}          # 벗겨짐·파손은 금속 노출로 이어진다
grade = 0 if kind == 0 else min(5, SIZE_G[size] + (1 if kind in KIND_UP else 0))
```

등급은 주관 판단이라 모델이 가장 쉽게 뭉갭니다. 크기·종류는 눈에 보이는 사실이라 훨씬 안정적이고,
이 규칙은 문서에 그대로 적을 수 있으며 같은 입력이면 같은 등급이 나옵니다.

> ⚠️ **계산은 하되 리포트에 내보내지 않습니다.** 결함/무결함 분리력이 0 이었습니다(카드 §5.6).

---

## 4. ③ 규칙 계산 (코드)

### 4.1 셀 영역 추정

```python
def cell_box(im, tol=55, frac=0.12):
    """배경(테두리 색)과 다른 픽셀이 몰린 구간 = 셀 몸통"""
```

좌우 가장자리 1/20 을 배경 표본으로 삼아 중앙값을 구하고, 그와 채널합 차이가 `tol` 을 넘는 픽셀의
행·열 비율이 `frac` 을 넘는 구간을 셀로 봅니다. **실패 시 이미지 전체.**

> 셀 크롭(HSV 채도 기반, 파이프라인 상류)과는 다른 함수입니다. 크롭은 카드 §3.1.

### 4.2 파생 필드

| 필드 | 계산 | 비고 |
| --- | --- | --- |
| 구역 | `(박스중심y − cb.top) / 셀높이 < 0.22` → `금속캡`, 아니면 `몸통` | |
| 셀_상단에서 | 같은 비율 (0~1) | |
| 셀면적비_퍼센트 | `박스면적 / 셀면적 × 100` | |
| 구분 | `≥1.0%` → `큼`, 아니면 `작음` | VLM 크기의 유효 해상도가 2단계라 2단계로 맞춤 |

---

## 5. ④ 출력 JSON

### 5.1 전제 — 리포트를 쓰는 것은 LLM 입니다

**이미지를 볼 수 없습니다.** 우리가 틀린 값을 넘기면 LLM 은 그대로 사실로 받아 적습니다.
스스로 의심할 방법이 없습니다. → 값만 넘기지 않고 **그 값이 어디까지 검증됐는지**를 같이 넘깁니다.

```
{OUT}/report_input/
    _contract.json        ← LLM 프롬프트 맨 앞에 붙일 계약서 (1개)
    cell_0041.json        ← 셀당 1개
```

**LLM 에는 `_contract.json` + 해당 `cell_XXXX.json` 을 같이 줍니다.**
계약서 없이 주면 LLM 이 유형후보 1등을 확정으로 써버립니다.

### 5.2 `_contract.json` — 전문

```json
{
  "설명": "배터리 셀 외관 검사 결과. 각 필드에 신뢰 등급이 붙어 있다.",
  "지켜야 할 것": [
    "신뢰='높음'인 값만 단정해서 쓴다.",
    "신뢰='낮음'인 값은 반드시 '~로 보이나 확정되지 않음' 형태로 쓴다. 단정하면 안 된다.",
    "유형후보는 순서만 있고 확률이 아니다. '가능성 있는 후보'로만 나열하고 퍼센트를 지어내지 않는다.",
    "유형후보가 2개 이상이면 모두 언급한다. 1등만 쓰고 나머지를 버리지 않는다.",
    "여기 없는 값을 지어내지 않는다. 특히 원인·조치는 우리가 판단하지 않았다.",
    "이미지를 보지 않고 쓰는 리포트임을 전제한다."
  ],
  "필드 신뢰 등급": {
    "판정":     {"신뢰": "높음", "근거": "P 0.855 / R 1.000 / F1 0.922 (사람 라벨 300장, 배포 경로)"},
    "위치":     {"신뢰": "높음", "근거": "점 recall 0.818, 순열 대조 2.51배 (사람 라벨 300장)"},
    "크기":     {"신뢰": "중간", "근거": "실측 면적과 순위상관 0.297 (p=0.001). 큼/작음 2단계만 유효"},
    "유형후보": {"신뢰": "낮음",
                "근거": "최빈 유형 베이스라인을 못 넘음 — top-1 +0.081 · top-2 +0.038 · top-3 -0.014",
                "주의": "유형은 참고용이다. 리포트에서 확정 표현을 쓰지 말 것",
                "점수_없는_이유": "신뢰도 점수를 낼 수 있었으나 캘리브레이션이 어긋나(평균 0.954 vs 실제 0.477) 내보내지 않는다. 후보 순서 외의 확신을 부여하지 말 것"}
  }
}
```

### 5.3 `cell_XXXX.json` 스키마

```json
{
  "셀": "0041",
  "검사_프레임수": 4,
  "주의": "프레임은 같은 셀을 360도 회전하며 찍은 것이다. 같은 결함이 여러 프레임에 나올 수 있다.",
  "프레임": [
    {
      "파일": "EXT__RGB_cell_cylindrical_0041_151__5219a299.jpg",
      "판정": "결함",
      "판정_신뢰": "높음",
      "판정_근거": {"임계": 0.10, "박스수": 18, "기준": "8개 이상"},
      "결함": [
        {
          "위치": {"구역": "몸통", "셀_상단에서": 0.73, "bbox": [0, 415, 297, 893]},
          "위치_신뢰": "높음",
          "크기": {"셀면적비_퍼센트": 10.83, "구분": "큼"},
          "크기_신뢰": "중간",
          "유형후보": ["긁힘·스크래치", "녹·부식", "파손·찢김"],
          "유형_신뢰": "낮음",
          "유형_주의": "검증 미통과. 확정 표현 금지"
        }
      ]
    }
  ]
}
```

필드 정의표는 카드 §5.4.

**결함 배열은 `프레임.판정 == "결함"` 일 때만 채워지고, 프레임당 최대 6개**(점수 상위).
유형후보는 `[색규칙/VLM 1등, 2등, (없으면) 검출기 태그]` 순이며 셋이 다르면 최대 3개까지,
**점수는 넣지 않습니다.**

---

## 6. 평가 프로토콜

### 6.1 평가셋

| 항목 | 1차 | 2차 |
| --- | --- | --- |
| 프레임 | 150장 | 150장 (1차와 **겹침 0**) |
| **합계** | \- | **300장 · 점 684개 · 무결함 99장** |
| 추출 | 셀당 최대 2장 층화, split=`val` 고정 | seed 2026, 1차 파일명 제외 |
| 용도 | 운영점 결정 · 규칙 탐색 | 최종 검증 전용 |

**셀당 최대 2장인 이유**: 한 셀의 270프레임은 서로 닮아 상관 표본이 됩니다(유효 표본 수가 줍니다).

### 6.2 지표

| | |
| --- | --- |
| 주지표 | **점(point) recall** — 사람 점이 예측 박스 안에 들어가면 적중 |
| 프레임 지표 | Precision / Recall / F1 (결함 vs 정상) |
| 부지표 | 장당 박스 수 · 유형별 recall · 무결함 프레임 오탐 · 박스 밀도 비대칭 |
| **금지** | **IoU 안 씁니다** |

**IoU 를 쓰지 않는 이유**: 기존 GT 는 결함 전체가 아니라 파편이라, 결함을 정확히 잡을수록 면적이
커져 IoU 가 떨어집니다. 점은 조각이 될 수 없고 크기 논쟁도 없습니다.

### 6.3 대조군 — 필수

**순열 대조**: 이미지 이름을 섞어 **남의 이미지 박스로 채점**합니다.
박스를 많이(또는 크게) 뿌리면 우연히 맞기 때문입니다.

| 지표 종류 | 판정 통계 | 이유 |
| --- | --- | --- |
| recall · 정확도 | **배율 ≥ 2** | 순열 바닥이 실제로 낮다(0.09~0.33) |
| F1 스윕 | **순열 p < 0.01 & F1 > p95** | 순열 바닥이 기저율 F1(~0.5)이라 배율 상한이 2.0 |
| 다중클래스 정확도 | **최빈 유형 베이스라인** 대비 | 순열은 "사소한 규칙보다 나은가"를 안 묻는다 |

**세 번째를 빠뜨리지 마세요.** 순열 대조를 통과해도 "가장 흔한 유형 찍기" 보다 못할 수 있습니다.

### 6.4 사전 등록

**지표와 폐기 규칙을 예측을 보기 전에 고정합니다. 결과를 보고 바꾸지 않습니다.**

---

## 7. 재현 절차

### 7.1 노트북

| 파일 | 셀 | 역할 |
| --- | --- | --- |
| `battery_ext_unsup.ipynb` | 12 | §0 크롭본 / **§1 OWLv2** / §2 육안 관문 / §3 셀 몽타주 / **§4 Qwen 로드** |
| `battery_ext_evalset.ipynb` | 30 | §A·§A2 표본 / §B 라벨링 / §C 채점 기반 / §C-run~§Y 산출 |
| `battery_ext_modelcard_check.ipynb` | 11 | revision · 환경 · 지연 · 운영점 · 골든 픽스처 |

### 7.2 실행 순서 (목적별)

```
리포트 JSON 생성 (운영)   unsup §1  →  evalset §C  →  §M3  →  §Y
게이트 성능 재현          unsup §1  →  evalset §C  →  §C-run
2단 VLM 덤프              unsup §1  →  unsup §4  →  evalset §C  →  §E  →  §F
평가셋 확장               evalset §A2  →  §B (WORKER='me2')
모델카드 재검증           modelcard_check §0→§1→§3→§4→§5→§7→§8→(§6)→§2→§9
```

⚠️ `unsup §4-run` 은 돌리지 마세요 — 셀 리포트 전용이며 **옛 프롬프트**를 씁니다.

### 7.3 산출물

| 경로 | 내용 |
| --- | --- |
| `{OUT}/images/` | 평가셋 이미지 300장 |
| `{OUT}/labels_me.json` · `labels_me2.json` | 사람 점 라벨 (1차·2차) |
| `{OUT}/sample_seed42_n150.json` · `sample2_seed2026_n150.json` | 표본 매니페스트 |
| `{OUT}/vlm_dump_v8.json` | VLM 추론 덤프 |
| `{OUT}/report_input/` | **리포트 LLM 전달용 JSON** |
| `{OUT}/modelcard/` | `env.json` · `revisions.json` · `latency_*.json` · `operating_point.json` · `golden_fixture*.json` |

`{OUT}` = `/content/drive/MyDrive/ext_evalset` (`kt_out` 이 읽기 전용이면 자동 폴백)

**레포에 커밋된 것** (재현에 필요한 고정값)

| 파일 | 내용 |
| --- | --- |
| `golden_fixture_deploy.json` | **배포 회귀** — 원본 20장 · thr 0.10 · 원본 좌표 |
| `golden_fixture.json` | 검출기 회귀 — 크롭 20장 · thr 0.08 · 크롭 좌표 |
| `fixtures_deploy/` | 배포 픽스처 원본 이미지 20장 (1920×1080) |
| `ext_requirements.lock.txt` | 위 픽스처를 만든 환경 (torch 2.11.0+cu128 · transformers 5.14.1 · pillow 11.3.0) |

픽스처 **이미지 20장은 `fixtures_deploy/` 에 있습니다** — `--images fixtures_deploy` 로 바로 돌립니다.

### 7.4 회귀 검사 — 배포·업그레이드 뒤 반드시

```bash
python ext_infer.py --selftest                       # 모델 없이 좌표·계약서
python ext_infer.py --verify-fixture     --fixture golden_fixture_deploy.json --images <원본 20장 폴더>
# 회귀 있으면 exit 1
```

**픽스처 2종은 서로 바꿔 쓸 수 없습니다.** 평가 픽스처는 크롭본(2세대 JPEG) 입력에 thr 0.08,
배포 픽스처는 원본(1세대) 입력에 thr 0.10 입니다. JPEG 압축 세대가 검출 수를 53% 바꾸기
때문입니다(카드 §6). 잘못 들이대면 도구가 `_meta.thr_gate` · `_meta.revision` 을 현재 `Config`
와 대조해 🔴 로 막습니다.

**실행 기록** — 2026-08-06 Colab GPU: **20/20 · 실패 0 · 경고 0**
(`{OUT}/modelcard/fixture_verify_deploy.json`).

판정은 **`gate` · `n_box` 완전 일치만 실패**, 좌표는 경고입니다 —
`crop_box` 를 400 px 썸네일에서 계산하므로 Pillow 버전에 따라 1~5 px 흔들립니다(카드 §7.3).

---

## 8. 금지사항

근거는 전부 카드 §9.2 · §11.2 에 실측으로 붙어 있습니다. **근거를 확인하지 않고 되돌리지 마세요.**

| | |
| --- | --- |
| 🔴 | **외부 API 호출 금지** — 배터리 도메인 제약. 두 모델 다 로컬 GPU |
| 🔴 | **매니페스트 라벨 사용 금지** — 맞는 게 거의 없습니다 |
| 🔴 | **유형 이름을 확정으로 리포트하지 말 것** — 검증 미통과 |
| 🔴 | **유형 신뢰도 점수를 외부로 내보내지 말 것** — 캘리브레이션 붕괴 |
| 🔴 | **`정상:금속캡` 을 `DROP_TAGS` 에 넣지 말 것** — 진짜 결함을 더 많이 잃습니다 |
| 🔴 | **`PIL.thumbnail()` 쓰지 말 것** — 확대를 안 합니다. `resize` |
| 🔴 | **프롬프트에 예시·기준표·특징 목록을 넣지 말 것** — 그대로 복사됩니다 |
| 🔴 | **예산(장당 N박스) 모드로 평가하지 말 것** — 정상 프레임에도 N박스를 강제해 신호를 가립니다 |
| 🔴 | **점 recall 을 IoU 기준 옛 수치와 나란히 놓지 말 것** — 다른 잣대입니다 |
